import { 提示词结构 } from '../../types';

export const 数值_世界演化: 提示词结构 = {
    id: 'stat_world_evo',
    标题: '世界演化法则',
    内容: `
<世界演化协议>
【世界演化协议｜执行队列版】
世界推进由“活跃 NPC 后台行动 + 待执行事件 + 进行中事件 + 世界镜头规划 + 时间流逝”共同驱动；只维护会改变后续局势的中高影响动态。
- 本协议处理的是“后台背景演变”，不是台前角色跟拍；当前在场角色的即时动作、对白承接、招式细节与现场走位，留给正文、变量与台前链路，不在这里重复追踪。

## 1. 时间职责
- 除 \`环境\` 外，世界侧结构化时间统一使用 \`YYYY:MM:DD:HH:MM\`。
- 活跃 NPC 主要看：\`行动开始时间 / 行动结束时间\`。
- 待执行事件主要看：\`计划执行时间 / 最早执行时间 / 最晚执行时间\`。
- 进行中事件主要看：\`开始时间 / 预计结束时间\`。
- 世界镜头主要看：\`触发时间\`。
- 描述文本可用古风时辰，但变量字段必须保持标准时间字符串。

## 2. 触发门槛
- 对待执行中的世界线，优先回答六个问题：这是什么、何时最早能启动、何时最迟仍有效、还缺哪些前置条件、命中哪些触发条件、会被哪些阻断条件拦下。
- 当前时间早于最早时间，不得提前执行。
- 当前时间已到，但前置条件或触发条件未成立，只能保持待执行或观察，不得硬推。
- 命中阻断条件时，必须延后、偏转或取消原路径推进。
- 超过最晚时间仍未执行的事件，要进入“错过 / 偏转 / 失效”判断。

## 3. 世界根树职责
- \`世界.活跃NPC列表\`：记录后台可追踪的 NPC 行动线。
- \`世界.待执行事件\`：记录尚未开始、但已经具备明确时间门槛和条件门槛的事件。
- \`世界.进行中事件\`：记录已经开始推进、仍在发酵中的事件。
- \`世界.已结算事件\`：记录已经落定结果的事件。
- \`世界.世界镜头规划\`：记录镜头外正在发生什么、哪些压力与氛围正在逼近。
- \`世界.江湖史册\`：只记录已落定且会长期影响后续走向的历史归档。
- \`世界.地图 / 世界.建筑\`：记录可长期复用的空间锚点。

## 4. 活跃 NPC 后台推进
- 每个 \`世界.活跃NPC列表[i]\` 至少包含：\`姓名 / 所属势力 / 当前位置 / 当前状态 / 当前行动 / 行动开始时间 / 行动结束时间\`。
- \`世界.活跃NPC列表\` 只追踪后台对象；主角本人不得进入该列表。
- \`世界.活跃NPC列表\` 只收“已开始执行”的后台行动；尚未开始、仍在等条件、仍在预热的线索，继续留在 \`待执行事件\` 或镜头规划中。
- \`当前行动\` 必须能回答“他此刻正在做什么”；不能留空，也不能只写抽象标签；只写当前这一个动作，不把历史过程累成数组。
- 若该 NPC 本回合已经在场、正在与主角直接互动、或后台动作被台前剧情打断，该条后台跟踪必须 \`set 当前状态 = "[打断]当前在场"\` 一类受阻状态；\`[打断]当前在场\` 只写在 \`当前状态\`，禁止写进 \`当前行动\`。此时 \`当前行动\` 只保留被打断前正在执行的后台动作说明或当前的受阻描述。
- 该 NPC 离场后若仍需追踪，应先删除旧打断项，再重新 \`push\` 一条完整新对象，不直接沿用旧打断项续写。
- \`行动开始时间\` 用来记录这条后台行动何时真正进入执行态；若仍未开始，就不应进入 \`世界.活跃NPC列表\`。
- 到达 \`行动结束时间\` 后，要推进结算、续段、迁移或删除，不让已开始的旧行动长期空转。
- NPC 行动完成时，应把结果沉淀到事件、镜头、史册或下一段行动里，而不是长期空转。
- 同一 NPC 同一时刻只保留一条主行动线，避免多地点多行动并发冲突。
- 同人模式下，活跃 NPC 优先来自原著已存在人物、原著势力链可合理外延的人物、或已被当前偏转事实明确生出的新人物。
- 同人模式下，原著角色的后台行动要同时贴合原著时期、身份立场、情报能力、行动半径与当前章节窗口。

## 5. 世界事件流
- 创建待执行事件时，至少补齐：\`事件名 / 类型 / 事件说明 / 计划执行时间 / 最早执行时间 / 最晚执行时间 / 前置条件[] / 触发条件[] / 阻断条件[] / 执行后影响[] / 错过后影响[] / 当前状态\`。
- 事件开始执行后，从 \`待执行事件\` 进入 \`进行中事件\`。
- 事件结算后，从 \`进行中事件\` 迁入 \`已结算事件\`。
- 只有会改变长期走向的结算事件，才允许再写入 \`世界.江湖史册\`。
- 同一事件不要同时长期停留在“待执行 / 进行中 / 已结算”多个池子里。
- \`世界.进行中事件\` 普通条数常态建议维持 5 条、峰值不超过 7 条；进入该池的事件至少要同时具备开始原因、当前进展与已产生影响。
- \`当前进展\` 是单条当前摘要，不是历史日志数组；事件推进时应重写当前摘要，而不是每回合把旧进展继续堆进去。
- \`世界.进行中事件\` 条数不足时，优先从“已满足启动条件的待执行事件 > 既有旧线续段 > 当前章节逼近出的世界前置状态 > 强因果新动向”补位。
- \`世界.进行中事件\` 条数高于峰值时，要在当回合完成结算、回退或清理，不把超额留到下一轮。
- \`世界.已结算事件\` 只保留近期结算缓冲层：普通条数常态建议维持 4 条、峰值不超过 6 条。
- 超额时按固定顺序清理：先归档可入史册项，再删除结果已沉淀的普通项，最后删除最旧、最弱、与当前章最远的项，直到回到阈值内。

## 6. 世界镜头规划
- \`世界.世界镜头规划\` 必须保留，不得删除。
- 每条镜头至少回答：什么时候能出现、为何会出现、关联谁、关联哪里、沉淀什么气氛或后果。
- 世界镜头负责承接场外动态、远端余波、氛围压迫与群像呼应；它不是正文事实的重复抄写。
- 未前台发生的镜头仍可保留在未来池，但要补齐触发条件与状态。

## 7. 同人联动
- 同人模式下，世界推进要优先贴住小说分解滑窗里的 \`【前一章节内容】 / 【当前章节内容】 / 【下一章节内容】\`。
- 与原著相关的世界事件、镜头规划，优先补 \`关联分解组\` 与 \`关联分歧线\`；活跃 NPC 则优先保持当前行动与时间闭合，不再把原著锚点元信息挂在正在执行的行动项里。
- 未到原著关键节点时，只允许写“逼近 / 发酵 / 预热 / 余波 / 试探”，不要提前把原著主结论写死。
- 当前章节未到的原著人物，不提前写成下一章节才成立的到场、出手、表态或情报掌握结果。

## 8. 地图与建筑
- \`世界.地图\` 负责可独立抵达、可长期复用的地点锚点，必须带 \`[x,y]\` 坐标字符串。
- \`世界.建筑\` 负责地点内部的微观构造、房间、院落、楼阁、后厨、地牢等内部节点。
- 若正文或世界演变形成了新的可长期抵达地点，应优先写入 \`世界.地图\`；只有内部构造才优先写入 \`世界.建筑\`。

## 9. 数量与补位
- 活跃 NPC、待执行事件、进行中事件、世界镜头都应保持“宁少勿空、宁实勿滥”。
- 补位优先顺序固定为：已成立旧线续写 > 已成立人物/势力/地点的下一段动作 > 当前章节逼近出的世界前置状态 > 强因果的新动向。
- 不得为了凑数量生造无因果空事件、空 NPC 或空镜头。
- 若当前没有足够可信来源，允许暂时低于常态，并在说明层明确缺口原因。

## 10. 玩家可感知联动
- 世界变化至少要在一个玩家可感知层落地：环境、社交、剧情章节状态、规划树、事件结果或历史沉淀。
- 只要时间推进，就要同步检查世界事件时效、NPC 行动时效与镜头窗口。
- 高收益世界结果必须同步附带代价、风险或后续压力。

## 11. 一致性底线
- 时间线单向推进。
- 同名事件、同名 NPC 尽量保持单一追踪对象。
- 当前章节未完成时，\`【下一章节内容】\` 只能作为预热参考，不能提前写成已发生事实。
- \`世界\` 中不再维护 \`势力常识\`、\`地理常识\` 这类世界观提示词字段。

</世界演化协议>
`,
    类型: '数值设定',
    启用: true
};
