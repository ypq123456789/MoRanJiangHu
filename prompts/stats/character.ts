import { 提示词结构 } from '../../types';
import { 默认累计境界分段映射提示词 } from '../runtime/fandom';
import { 构建修炼体系附加块 } from '../../utils/promptFeatureToggles';

export const 数值_角色属性: 提示词结构 = {
    id: 'stat_char',
    标题: '角色属性总纲',
    内容: `
<角色属性总纲协议>
【角色属性总纲｜字段口径、五项上限、六维职责】
本协议负责 \`角色\` 域的合法更新，字段树以 \`<数据结构定义>\` 为准，修炼/战斗/恢复等专项公式由各自协议接管。

## 1. 角色字段口径
- 基础身份：\`姓名 / 性别 / 年龄 / 出生日期 / 外貌 / 性格 / 称号\`
- 六维：\`力量 / 敏捷 / 体质 / 根骨 / 悟性 / 福源\`
- 生存资源：\`当前精力 / 最大精力 / 当前饱腹 / 最大饱腹 / 当前口渴 / 最大口渴\`
- 承载：\`当前负重 / 最大负重\`
- 战斗体征：七部位 \`当前血量 / 最大血量 / 状态\`
- 资源对象：\`金钱 / 装备 / 物品列表 / 玩家BUFF\`
- 统一纪律：只用现有键名，写入保持在既有字段树内，如 \`金币\`、\`蓝量\`、\`生命值池\` 这类树外路径不进入命令
${构建修炼体系附加块([
'- 修炼轴：`境界 / 境界层级 / 当前经验 / 升级经验 / 突破条件`',
'- 生存资源：`当前内力 / 最大内力`',
'- 资源对象：`功法列表`'
].join('\n'))}

${构建修炼体系附加块(`
## 2. 累计境界值接入
- 角色成长统一采用累计境界值体系：
${默认累计境界分段映射提示词}
- 凡人/普通人/未入境属于正式映射值 \`1\` 之前的概念层，可在叙事中使用，但不占用正式累计境界值。
- \`角色.境界层级\` 记录正式境界值；\`角色.境界\` 记录对应的境界文案。
- 境界文案与境界值同回合同步，开脉、聚息至化罡、通玄以上分别使用各自固定写法。
`)}

## 3. 数值更新硬约束
- 任意 \`add\` 后都必须裁切：\`当前值 >= 0\`，且有上限时 \`当前值 <= 最大值\`
- \`精力\` 负责体能表现与基础恢复
- 金钱字段保持非负数
- 正文事实与命令同步落地，让叙事、变量与结果保持一致
- 高收益必须同时具备时间、风险、资源代价或剧情依据
${构建修炼体系附加块('- `精力` 与 `内力` 是双资源系统：前者偏体能，后者偏内息与功法。')}

## 4. 五项上限与重算
- 统一派生公式：
  - \`最大精力 = floor(36 + 体质*6.2 + 根骨*3.4)\`
  - \`最大饱腹 = floor(72 + 体质*2.2 + 力量*1.2)\`
  - \`最大口渴 = floor(72 + 体质*2.1 + 根骨*1.3)\`
  - \`最大负重 = floor(82 + 力量*10.5 + 体质*2.4)\`
- 重算触发时机：
  - 六维变化，尤其力量、体质、根骨、悟性
  - 会改变承载能力的长期 BUFF 或伤病结算
- 重算纪律：
  - 每个上限单独 \`set\`
  - 若重算后当前值超上限，同回合裁切到上限
${构建修炼体系附加块([
'  - `最大精力 = floor(36 + 体质*6.2 + 根骨*3.4 + 境界层级*5.2 + max(0,境界层级-4)*2.2 + max(0,境界层级-8)*2.6 + max(0,境界层级-12)*3.1 + max(0,境界层级-16)*3.8 + max(0,境界层级-20)*4.8 + max(0,境界层级-24)*6.0 + max(0,境界层级-27)*7.2 + max(0,境界层级-33)*9.0)`',
'  - `最大内力 = floor(18 + 根骨*7.4 + 悟性*6.6 + 境界层级*6.0 + max(0,境界层级-4)*2.6 + max(0,境界层级-8)*3.2 + max(0,境界层级-12)*4.0 + max(0,境界层级-16)*5.0 + max(0,境界层级-20)*6.4 + max(0,境界层级-24)*8.2 + max(0,境界层级-27)*9.6 + max(0,境界层级-33)*12.0)`',
'  - `最大饱腹 = floor(72 + 体质*2.2 + 力量*1.2 + 境界层级*2.8 + max(0,境界层级-4)*0.7 + max(0,境界层级-8)*0.8 + max(0,境界层级-12)*1.0 + max(0,境界层级-16)*1.2 + max(0,境界层级-20)*1.5 + max(0,境界层级-24)*1.9 + max(0,境界层级-27)*2.2 + max(0,境界层级-33)*2.8)`',
'  - `最大口渴 = floor(72 + 体质*2.1 + 根骨*1.3 + 境界层级*2.8 + max(0,境界层级-4)*0.7 + max(0,境界层级-8)*0.8 + max(0,境界层级-12)*1.0 + max(0,境界层级-16)*1.2 + max(0,境界层级-20)*1.5 + max(0,境界层级-24)*1.9 + max(0,境界层级-27)*2.2 + max(0,境界层级-33)*2.8)`',
'  - `最大负重 = floor(82 + 力量*10.5 + 体质*2.4 + 境界层级*2.4 + max(0,境界层级-4)*1.2 + max(0,境界层级-8)*1.4 + max(0,境界层级-12)*1.8 + max(0,境界层级-16)*2.2 + max(0,境界层级-20)*2.8 + max(0,境界层级-24)*3.5 + max(0,境界层级-27)*4.0 + max(0,境界层级-33)*5.0)`',
'- 同段内提升仍可感知；每逢 `4/8/12/16/20/24/27/33` 节点，上限基线会明显抬升，用于体现跨大境断层。',
'  - `角色.境界层级` 变化',
'- 境界提升后同步刷新五项上限，保持成长链条完整'
].join('\n'))}

## 5. 六维职责
- \`力量\`：破甲、搬运、硬碰硬压制、负重承载
- \`敏捷\`：先手、命中、闪避、位移
- \`体质\`：血量上限、抗伤、持续作战
- \`根骨\`：恢复稳定性与身体承载
- \`悟性\`：学习成长、理解速度与破招理解
- \`福源\`：奇遇与稀有概率修正，但不是保底成功
${构建修炼体系附加块([
'- `根骨`：经脉承载、恢复稳定性、内息容纳',
'- `悟性`：功法学习、熟练成长、破招理解'
].join('\n'))}

## 6. 六维成长边界
- 六维六项都允许变化，不仅是 \`福源 / 悟性 / 根骨\`
- 增长触发建议：
  - 高强度战斗与长期训练：优先驱动 \`力量 / 敏捷 / 体质\`
- 成长节奏建议：
  - 非突破回合：单回合单项最多 \`+1\`
- 六维在长期推进中保持可成长节奏，单回合增量收束在合理范围内
${构建修炼体系附加块([
'  - 同境升重成功：六维总计通常 `0~1`',
'  - 阶段跃迁成功：允许六维总计 `+1~+3`',
'  - 悟道、传承、机缘：优先驱动 `悟性 / 根骨 / 福源`',
'- 单项软上限建议：`floor(8 + 境界层级*1.2 + max(0,境界层级-4)*0.7 + max(0,境界层级-8)*0.8 + max(0,境界层级-12)*0.9 + max(0,境界层级-16)*1.0 + max(0,境界层级-20)*1.2 + max(0,境界层级-24)*1.5 + max(0,境界层级-27)*1.8 + max(0,境界层级-33)*2.2)`，超出需强依据',
'  - 同境升重回合：通常不超过总计 `+1`',
'  - 阶段跃迁回合：总增量仍建议 `+1~+3`'
].join('\n'))}

## 7. 与其他模块联动
- 战斗实时结算：以 \`<战斗口径协议>\` 与 \`<部位生命协议>\` 为准
- 经验与升级：以 \`<经验成长协议>\` 为准
- 恢复与治疗：以 \`<恢复休整协议>\` 为准
- 掉落与经济：以 \`<掉落资源协议>\` 为准
${构建修炼体系附加块('- 修炼、升重、突破、反噬：以 `<修炼体系协议>` 为准')}

## 8. 关键同步纪律
${构建修炼体系附加块([
'- `角色.境界层级` 必须存在且为数值',
'- `角色.境界` 必须能读出“境界名称 + 当前阶段”语义',
'- 若 `角色.境界层级` 已变化，`角色.境界` 必须同回合同步',
'- 阶段推进与大境突破按 `<修炼体系协议>` 的正式跳转表更新'
].join('\n'))}

## 9. 命令落地提醒
- \`add 角色.当前经验 = 36\`
- \`add 角色.当前精力 = -18\`
- \`set 角色.胸.状态 = "中伤"\`
- \`set 角色.金钱.银子 = 27\`
- \`set 角色.最大精力 = 132\`
- \`set 角色.最大负重 = 198\`
- \`set 角色.当前精力 = 132\`（仅当超上限时）
${构建修炼体系附加块([
'- `add 角色.当前内力 = -24`',
'- `set 角色.最大内力 = 146`',
'- `set 角色.境界 = "归元境中期"`',
'- `set 角色.境界层级 = 15`（与 `归元境中期` 对应）'
].join('\n'))}
</角色属性总纲协议>
`,
    类型: '数值设定',
    启用: true
};
