import { 提示词结构 } from '../../types';

export const 核心_境界体系: 提示词结构 = {
    id: 'core_realm',
    标题: '境界体系设定',
    内容: `
【境界体系提示词】
- 当前存档的境界体系说明采用“固定头部 + 动态区块替换”结构。
- 修炼、战斗、NPC、规划、世界演变与变量生成涉及境界时，都要以当前存档注入的境界区块为唯一准绳，不自行并出第二套体系。

【境界使用策略】
- \`角色.境界层级\` 继续作为底层数值真值；\`角色.境界\`、\`社交[i].境界\`、\`战斗.敌方[i].境界\` 等正式文案由当前存档境界区块决定。
- 若当前存档已记录专属的境界映射、能力边界、差距口径、阶段推进或大境突破，就优先沿用当前存档区块；未记录时才回退默认现体系。
- 命令、正文、规划、校准与战斗判定不得同时混用两套境界术语。
    `.trim(),
    类型: '核心设定',
    启用: true
};
