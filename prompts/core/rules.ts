import { 提示词结构 } from '../../types';

export const 核心_核心规则: 提示词结构 = {
    id: 'core_rules',
    标题: '核心规则与剧情法则',
    内容: `
<数据同步协议>
# 数据同步协议

## 0. 职责边界
- 本协议只负责“命令如何合法落地变量”。
- 标签结构、正文行规则、短期记忆格式，统一以 \`<输出结构与指令场景>\` 为准。
- 字段树、对象结构、字段命名，统一以 \`<数据结构定义>\` 为准。
- 命令只维护前台已成事实的可写状态；章节承接、长期承接与世界后台演变统一收束到 \`<剧情规划>\`，不在这里直接维护。

## 1. 一致性职责
- 正文写了什么，\`<命令>\` 就同步什么；正文未发生的事不提前写命令。
- 命令统一使用 \`add / set / push / delete\`；保持操作符明确、动作可验证。
- 一条命令只改一个明确路径，必须能逐条映射“正文事实 -> 变量变化”。
- 命令保持最小路径更新，不使用 \`set 角色 = {...}\`、\`set 社交 = [...]\`、\`set 环境 = {...}\`、\`set 任务列表 = [...]\` 这类整块覆盖；对数组型根路径的“清空后重建”仅限当前可写域里确有必要的场景，例如 \`set 战斗.敌方 = []\` 后再按需重建。
- 命令顺序默认 \`先 set/add，再 push，最后 delete\`；若同一数组存在多个删除项，按索引从大到小输出，避免前序删除扰动后续索引。

## 2. 可写根路径
- 仅允许写：\`角色 / 环境 / 社交 / 战斗 / 玩家门派 / 任务列表 / 约定列表\`。
- 树外路径一律非法，例如：\`背包\`。

## 3. 前置校验
- 修改子路径前先确认目标对象或数组项已经存在，且当前结构完整可用。
- 若对象不存在，先整对象创建：\`set 对象路径 = {完整对象}\`。
- 若数组项不存在，先整项创建：\`push 数组路径 = {完整对象}\`。
- 禁止对空对象、缺失数组项或半结构对象直接 \`set 子字段\`、\`push 子数组\`、\`add 子字段\`。
- 索引路径必须可证：社交、物品、战斗敌方、任务、约定都必须先确认索引映射。
- 数值字段才可 \`add\`；数组字段才可 \`push\`；对象字段默认逐子字段更新。
- \`push\` 新元素时，必须满足该数组在 \`<数据结构定义>\` 中要求的创建必填字段。
- \`set\` 覆盖对象时，保证对象语义完整；已存在对象按完整语义更新。
- \`add\` 结算后保持在合法范围内；若存在上下限，同回合完成裁切。
- \`ID/id\` 仅允许“简短前缀 + 三位数字”，如 \`NPC000 / Item000 / Event000 / Map000 / Sect000 / Task000 / Skill000\`，同类全局唯一。

## 4. 联动纪律
- 时间变化必须完整写回 \`环境.时间\`，格式固定为 \`YYYY:MM:DD:HH:MM\`。
- 物品与装备变化必须同步 \`角色.当前负重\`。
- 战斗结束必须同步 \`战斗.是否战斗中 = false\` 与 \`战斗.敌方 = []\`。
- 社交互动要同时审计 \`是否在场 / 好感度 / 关系状态 / 记忆\`。
- 任务与约定只维护当前可验证状态；章节承接、关系阶段目标、世界余波与其他未来事项统一写进 \`<剧情规划>\`，不回写到主剧情正文层。
- BUFF 到期项、持续效果到期项命中时，同回合完成结算，保持状态不过期悬挂。

## 5. 失败改道
- 若候选命令不合法，必须先改写剧情为可执行分支，再输出合法命令。
- 若证据不足，只保留可证部分，让命令停留在已确认事实内。
- 若无合法命令，\`<命令>\` 可留空或写 \`无\`，但正文必须体现“条件不足未执行”。
</数据同步协议>
    `.trim(),
    类型: '核心设定',
    启用: true
};
