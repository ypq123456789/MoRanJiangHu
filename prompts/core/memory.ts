
import { 提示词结构 } from '../../types';
import { 构建修炼体系附加块 } from '../../utils/promptFeatureToggles';

export const 核心_记忆法则: 提示词结构 = {
    id: 'core_memory_rules',
    标题: 'NPC记忆写入法则',
    内容: `<NPC记忆写入法则>
【NPC 记忆写入法则】
**核心职责**: NPC 不是只会说话的木偶，他们拥有基于 \`NPC结构\` 的持久化记忆。

1. **数据定位**:
   - 所有的 NPC 数据存储在 \`社交\` 数组中。
   - 路径格式: \`社交[Index].记忆\`。

2. **建档与写入记忆 (Essential)**:
   - 对**已建档** NPC：每一次有效互动（对话、交易、战斗、委托、H事件）结束后，必须先审计该 NPC 是否因时间间隔与正文事实出现“档案刷新需求”，再决定追加记忆与字段刷新。
   - 对**未建档** NPC：先判定是否入档。
     - 一次性路人（如“临时路人”）默认不入档，不写社交记忆。
     - 当角色在剧情中有关键作用，或与玩家累计互动超过 3 次（基于即时剧情回顾/即时记忆）时，允许入档。
   - 入档前必须先查重（同名优先，必要时仅核对当前数组索引）：
     - 命中已有角色：更新原角色，保持单一社交档案，不重复 \`push 社交\`。
     - 未命中且满足入档条件：先 \`push 社交\` 创建，再写 \`社交[i].记忆\`。
   - 新入档时需补写此前关键互动记忆，再追加本回合记忆，避免记忆断层。
   - 对已建档 NPC，若距上次互动已过去足够长时间，且正文存在可验证变化，不能只补 1 条间隔记忆；必须同步用多条 \`set 社交[i].字段 = ...\` 刷新当前档案字段。
   - 长间隔重登场时，优先审计：\`年龄 / 外貌 / 身份 / 所属势力 / 地点 / 伤势状态 / 关系态度 / 是否在场\`；有正文依据才刷新，无依据则保持原值。
${构建修炼体系附加块('   - 长间隔重登场时，若启用修炼体系，再同步审计：`境界`。')}
   - 指令格式: 
     \`push 社交[TargetIndex].记忆 { "内容": "...", "时间": "317:03:16:09:45" }\`
   - **时间格式**: 使用标准串 \`YYYY:MM:DD:HH:MM\`。

3. **扁平化属性更新**:
   - 当发生H事件时，直接更新根节点下的属性。
   - 示例:
     - \`set 社交[0].是否处女 false\`
     - \`set 社交[0].初夜夺取者 "玩家名"\`

4. **记忆的应用**:
   - 生成对话前，必须检索该 NPC 的 \`记忆\` 列表。
   - 根据 \`胸部描述/小穴描述/屁穴描述/性癖/敏感点\` 与 \`是否处女\` 状态调整 NPC 的羞耻度和反应。
   - \`胸部描述/小穴描述/屁穴描述\` 视为常态档案字段：默认读取沿用，仅在可验证“质变事件”后再按新常态改写字段值。
</NPC记忆写入法则>`,
    类型: '核心设定',
    启用: true
};
