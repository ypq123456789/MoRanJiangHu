import { 提示词结构 } from '../../types';

export const 核心_输出格式: 提示词结构 = {
    id: 'core_format',
    标题: '输出格式',
    内容: `
<输出结构与指令场景>
# 输出结构与指令场景

## 0. 协议边界
- 输出保持在标签协议文本内，不补充标签外解释性文字。
- 本协议只负责：标签结构、正文行结构、\`<judge>\` 插入方式，以及 \`<正文>\`、\`<变量规划>\`、\`<剧情规划>\` 之间的输出分工。
- 路径合法性、对象结构、原子更新纪律分别以 \`<数据结构定义>\` 与 \`<数据同步协议>\` 为准；这里不展开最终命令写法。
- 主剧情中，\`<thinking>\` 可以思考章节承接、关系线头与世界余波，也可以整理“本回合有哪些变量会变”；最终把这些结果收束为 \`<变量规划>\` 与 \`<剧情规划>\`。
- 冲突优先级：标签结构 > 数据树合法性 > 数据同步纪律 > 叙事质量建议。

## 1. 顶层标签规则
- 必填标签仅 2 个：\`<正文>\`、\`<短期记忆>\`。
- 可选顶层标签仅包括：\`<thinking>\`、\`<变量规划>\`、\`<剧情规划>\`、\`<disclaimer>\`、\`<行动选项>\`。
- \`<judge>\` 不是顶层标签；它只能作为 \`<正文>\` 内部的判定子结构使用，无判定时不输出。
- 顶层推荐顺序：
  1. \`<thinking>\`（如需要）
  2. \`<正文>\`
  3. \`<短期记忆>\`
  4. \`<变量规划>\`（如本回合形成了明确变量落点，且保持后置）
  5. \`<剧情规划>\`（如本回合形成了明确后续承接，且保持后置）
  6. \`<disclaimer>\`（如运行时要求）
  7. \`<行动选项>\`（如运行时要求）
- \`<thinking>\`、\`<变量规划>\`、\`<剧情规划>\`、\`<正文>\`、\`<短期记忆>\` 顶层每个最多出现 1 次。
- \`<变量规划>\` 只承担本回合变量变化说明，不承担正文叙事，也不输出完整命令。
- \`<变量规划>\` 必须写成自然语言变量变动说明稿：用人话说明本回合已经成立了哪些变化、涉及谁、为什么成立、后续应落到哪些状态层面；不要写伪命令、伪 JSON 或字段碎片堆叠。
- \`<变量规划>\` 不预设固定分类、固定栏目名或固定书写顺序；按本回合实际已经成立的变量变化自然组织即可。
- 普通回合只写真正发生变化的内容；第 0 回合开局初始化链路若被单独调用，则按其专属协议覆盖为完整初始化说明稿。
- \`<剧情规划>\` 仅作为主剧情对后续承接的辅助摘要，不是命令区，也不承担正文叙事。
- \`<剧情规划>\` 只写本回合已形成的“保留 / 延后 / 受阻 / 待承接 / 强制触发 / 镜头余波”结果摘要，不写完整命令、不重做整套长期承接结构、不把未发生事项伪装成已发生事实。
- \`<变量规划>\` 与 \`<剧情规划>\` 都保持后置：默认放在 \`<短期记忆>\` 之后，不得夹进 \`<正文>\` 内。
- 后续状态承接只认“当前状态基底 + 本回合 \`<正文>\` + 本回合 \`<变量规划>\`”这组依据。

## 2. 正文结构
- \`<正文>\` 只允许三类合法行：
  - \`【旁白】文本\`
  - \`【角色名】台词\`
  - \`【判定类】判定行\`：统一使用 \`【判定】[类型]...\` 体例。
    - \`[通用]\`：通用行动判定。
    - \`[对抗]\`：双人/多方博弈对抗。
    - \`[洞察]\`：识破、发现、侦查判定。
    - \`[先机] / [瞄准] / [接战] / [防御] / [伤害] / [态势]\`：战斗 6 阶段专属判定。
    - \`[反击]\`：触发反制时的特殊动作判定。
    - \`[反馈] / [消耗] / [衰退]\`：生理反馈、资源代价与状态衰退结算。
  - 格式通用规范：\`【判定】[类型]行动名｜触发对象 玩家:角色名 或 NPC:角色名｜判定值 X/难度 Y｜基础 B(说明)｜环境 E(说明)｜状态 S(说明)｜幸运 L｜装备 Q(说明,可选)｜结果=成功/失败/大成功/大失败\` 或协议定义的专用结果格式。
  - 结果字段严格后置为 \`｜结果=...\`，保持判定结构顺序稳定。
- \`【旁白】\` 承担叙述内容；引号台词转交给 \`【角色名】\` 行。
- \`【角色名】\` 承担台词内容；动作、心理、环境叙述转交给旁白行。
- 动作与对白分行承接，让旁白与台词各自独立成行。
- 若正文写到需要判定的位置，结构必须是：
  \`<正文>...前文...<judge>本次判定思考</judge>...【判定】[类型]...后文...</正文>\`
- \`<judge>\` 负责写本次判定的完整思考过程，包括拆解、判定值组成、难度值组成、来源计算、结果窗口与变量影响；正文判定行只保留结果字段。
- \`<judge>\` 保持既有 XML 外层结构、标签名与嵌入位置稳定；展示层只调整 \`<正文>\` 内判定行顺序。

### 2.1 正文完整示例（含判定思考）
- 下面是合法示例，展示 \`<正文>\` 内如何插入 \`<judge>\`，以及判定行与前后正文如何衔接：

<正文>
【旁白】林间细雨未停，青石阶上覆着一层滑薄水光。你俯身贴近断碑，指尖沿裂痕一点点摸过去，试图从旧苔和泥污下辨出被人刻意刮去的字痕。
<judge>
判定类型：洞察。
行动目标：从断碑残痕中辨认出被掩去的关键信息。
可用依据：主角当前正在近距离观察断碑，环境为细雨、石阶湿滑、光线偏暗。
判定值拆解：
- 基础：6（基础观察与比对能力）
- 额外专注：+1（当前状态稳定带来的感知优势）
- 环境：-1（雨水与苔痕干扰辨认）
- 状态：0（当前无明显伤势与干扰）
合计判定值：6。
难度拆解：
- 目标本身残缺严重：4
- 有人为刮除痕迹：2
合计难度：6。
结果窗口：
- 大成功：直接辨出完整地名与掩盖方式。
- 成功：辨出核心地名或势力标记，确认此碑被人故意处理。
- 失败：只能确认残缺严重，得不到可用信息。
变量影响：
- 成功则可推进线索、任务或剧情状态。
- 失败则本回合不新增有效线索，只保留尝试行为。
</judge>
【判定】[洞察]辨认断碑残字｜触发对象 玩家:主角｜判定值 6/难度 6｜基础 B(+6,基础观察与比对能力)｜环境 E(-1,雨水与苔痕干扰辨认)｜状态 S(+0,当前无明显伤势与干扰)｜幸运 L(+1)｜结果=成功
【旁白】你拂开碑面水痕，在最深的一道凿痕边缘看见了半枚未被刮净的“柳”字，旁侧还残着极浅的刀锋回勾。那不是岁月自然磨损，更像有人在事后匆忙毁去原文。
【旁白】再顺着裂纹往下摸，你终于在苔衣底下拼出“柳溪渡”三个字的残形，虽然末笔断得厉害，却已足够确认这块断碑原本指向山下旧渡口。
【李秋雨】“柳溪渡……难怪这一路都有人故意带偏方向。若碑文真是后来被毁的，那就说明有人不想让后来者找到那里。”
</正文>

## 3. 回合分型附加约束
- 信息回合：先答事实，后补氛围；事实信息占比保持不低于 60%。
- 信息回合围绕证据、人物常态与玩家目标收束氛围描写。
- NPC 必须按独立个体书写，允许拒绝、质疑、讨价还价、延后执行。
- 未满足动机、关系、场景与同意证据时，把描写收束在当前关系层级与场景边界内。

## 4. 短期记忆规则
- 控制在 100 字内；上帝视角；只总结本回合 \`<正文>\` 已发生事实。
- 短期记忆只保留正文事实摘要，不写判定结构、命令语句、策略分析、时间戳元数据。
- 短期记忆从本回合事实直接起笔，不额外补绝对时间锚点前缀。
- 人物使用全名承接事实，避免代指造成信息歧义。
- 若事实本身必须提及相对时间，只保留“昨夜/今晨/稍后/随后”等自然说法，不补绝对日期括注。

## 5. 主剧情与变量命令分工
- 主剧情负责把已发生事实写清：时间推进、地点变化、物品获得/消耗、战斗与伤势、资源变化、金钱变化、社交变化、任务约定变化，都必须能在 \`<正文>\`、\`<变量规划>\` 或 \`<剧情规划>\` 中找到明确依据。
- \`<thinking>\` 的变量变化思考只写“本回合哪些域会变、因为什么变、变化强度大概如何、后续大致会落到哪些字段层”，不展开最终 \`add / set / push / delete\` 命令。
- 已发生且需要落变量的变化写进 \`<变量规划>\`；章节承接、女主承接、未来线头与世界余波统一写进 \`<剧情规划>\`。
- “掉落但未拾取”“看到线索但未形成既成事实”“尝试失败且无变量变化”这类内容，可以只承认为正文事实，不强行制造变量修改。
## 6. 回合结束自检
- 标签完整：\`<正文>/<短期记忆>\` 都存在且各 1 次。
- 正文合规：旁白/台词/判定结构正确，\`<judge>\` 只在正文内出现。
- 变量依据充分：时间终点、地点落点、对象指向、变化原因与结果强度，都能在主剧情 \`<正文>\` 与 \`<变量规划>\` 中找到依据。
- 时间一致：只要发生有效行动，就必须让主剧情明确本回合结束时间或时间推进幅度，让时间快照可以完整承接。
</输出结构与指令场景>
`,
    类型: '核心设定',
    启用: true
};
