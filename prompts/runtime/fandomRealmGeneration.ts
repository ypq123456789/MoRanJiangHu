import type { OpeningConfig } from '../../types';
import {
    默认累计境界差距口径提示词,
    默认累计境界大境突破提示词,
    默认累计境界大境突破跳转列表,
    默认累计境界武侠硬边界提示词,
    默认累计境界终点文案提示词,
    默认累计境界能力边界提示词,
    默认累计境界九阶命名提示词,
    默认累计境界映射数值列表,
    默认累计境界阶段推进提示词,
    默认累计境界阶段推进跳转列表
} from './fandom';

const 读取同人配置 = (openingConfig?: OpeningConfig | null) => {
    const fandom = openingConfig?.同人融合;
    const title = typeof fandom?.作品名 === 'string' ? fandom.作品名.trim() : '';
    if (!fandom?.enabled || !title) return null;
    return {
        title,
        sourceType: fandom.来源类型,
        fusionStrength: fandom.融合强度,
        keepCanonicalCharacters: fandom.保留原著角色 === true
    };
};

export const 同人境界体系生成系统提示词 = `
你是 WuXia 项目的同人境界体系生成器。任务是只生成“可替换的独立境界区块”。

【输出要求（必须）】
1. 只输出一个 \`<境界体系>...</境界体系>\` 标签块，不附加 JSON、Markdown 解释、注释或命令。
2. \`<境界体系>\` 内文本必须是可直接替换进项目现有境界协议的区块集合，不要重写固定头部。
3. 这次任务只负责境界体系，不重复生成世界观正文、开场剧情、人物设定、任务线或变量命令。
4. 底层累计境界值仍是项目固定真值，生成任务只负责“数值 -> 同人术语/规则”的完整映射与解释。
5. 若原著没有完整境界体系，允许在不违背原著强弱关系的前提下做最小补段，但必须明确是“原著优先，不足补段”，不能胡乱仙侠化。
6. 原著是第一依据；你要直接围绕作品原著生成体系，不等待世界观正文先写出来。
7. 你要做的是“把原著力量体系映射进当前项目的累计境界骨架”，不是“从当前世界观里摘几段力量描述再拼成境界稿”。
8. 世界观阶段只负责写力量边界概述；你的职责是单独产出完整、可执行的境界体系母板。
9. 这份结果会直接先服务开局生成，因此低阶到高阶都必须可落地，不能只写抽象顶层设定。

【必须覆盖的区块】
1. \`【境界映射母板】\`
2. \`【九阶命名与能力边界】\`
3. \`【境界差距口径】\`
4. \`【终点文案】\`
5. \`【阶段推进表】\`
6. \`【大境突破表】\`
7. \`【武侠硬边界】\`

【硬约束】
1. \`【境界映射母板】\` 中必须连续覆盖这些累计值：${默认累计境界映射数值列表.join(' / ')}。
2. 每一行映射都使用 \`数值 => 文案\` 格式。
3. \`【阶段推进表】\` 必须显式覆盖这些跳转：${默认累计境界阶段推进跳转列表.join('、')}。
4. \`【大境突破表】\` 必须显式覆盖这些跳转：${默认累计境界大境突破跳转列表.join('、')}。
5. \`【终点文案】\` 必须说明每个大段的终点写法，方便系统判断何时允许跨段突破。
6. 保持凡俗武道/原著上限，不把同人境界写成常态飞天、法术轰击、起死回生。
7. 如果原著大境比项目更粗，可以在同一原著大境内优先补出初期/中期/后期/圆满四段；高境不足时再收束为三段或单段，但必须服务于数值映射，不得违背原著相对强弱。
9. 若原著已有明确境界名、等级名、战力阶梯、称号梯度或高手层次，优先用原著术语；当前项目默认术语只可作为结构参考，不可反客为主。
10. 严禁输出地理、势力、社会环境、主线梗概等世界观正文内容；若出现大段世界观复述，视为失败。
11. \`【九阶命名与能力边界】\` 区块内必须保留两个子标记：\`- 九阶命名顺序固定：\` 与 \`- 境界能力边界：\`，方便系统在运行时替换原协议中的对应位置。
`.trim();

export const 构建同人境界体系生成用户提示词 = (params: {
    openingConfig?: OpeningConfig | null;
}): string => {
    const fandom = 读取同人配置(params.openingConfig);

    return [
        '【本次任务】',
        fandom
            ? `- 你需要以《${fandom.title}》为基础，按照当前项目的境界模板结构，定制一套贴合原著的同人境界体系提示词。`
            : '- 你需要以参考作品原著为基础，按照当前项目的境界模板结构，定制一套贴合原著的同人境界体系提示词。',
        '- 当前项目的累计境界结构只作为映射骨架，不是让你沿用默认术语。',
        '- 本次输出会被系统拿去替换现有协议中的默认境界区块，不要重写固定头部，也不要把世界观正文复制一遍。',
        '- 这份输出会先用于第0回合开局，所以必须让主角初始境界、开场出场 NPC、门派前辈、潜在敌手与第一幕冲突都能直接按原著体系落位。',
        '- 目标是让后续开局、主剧情、NPC、规划、世界演变、变量生成都能读取同一份同人境界规则。',
        fandom
            ? `- 参考作品：${fandom.title}（${fandom.sourceType}），融合强度：${fandom.fusionStrength}，保留原著角色：${fandom.keepCanonicalCharacters ? '是' : '否'}。`
            : '- 当前未提供有效同人配置；若无法提取原著体系，就回退现体系。',
        '',
        '【原著优先生成纪律】',
        fandom
            ? `- 先根据《${fandom.title}》识别原著已有的境界名、高手层次、等级称呼、强弱断层与能力上限。`
            : '- 先根据参考作品原著识别已有的境界名、高手层次、等级称呼、强弱断层与能力上限。',
        '- 若原著有明确境界体系，就直接以原著术语为核心，再映射到当前项目固定累计值。',
        '- 若原著只有模糊强弱阶梯，没有完整分段，就在不违背原著的前提下做最小补段。',
        '- 默认回退骨架优先采用四段制，也就是单个大境尽量写成初期 / 中期 / 后期 / 圆满，不要默认把首境扩成十重。',
        '- 直接依据原著完成体系生成，不等待世界观正文。',
        '- 输出时检查自己是否在复述地理、势力、社会、剧情梗概等世界观正文信息；如果是，删除重写。',
        '',
        '【当前项目映射骨架参考】',
        `- 必须覆盖的累计值：${默认累计境界映射数值列表.join(' / ')}。`,
        '【九阶命名与能力边界｜结构参考】',
        默认累计境界九阶命名提示词,
        默认累计境界能力边界提示词,
        '【境界差距口径｜结构参考】',
        默认累计境界差距口径提示词,
        '【终点文案｜结构参考】',
        默认累计境界终点文案提示词,
        '【阶段推进表｜结构参考】',
        默认累计境界阶段推进提示词,
        '【大境突破表｜结构参考】',
        默认累计境界大境突破提示词,
        '【武侠硬边界｜结构参考】',
        默认累计境界武侠硬边界提示词,
        fandom
            ? `- 上述内容是“结构骨架参考”。你需要把默认术语替换成最贴近《${fandom.title}》原著的术语、能力边界与断层说明。`
            : '- 上述内容是“结构骨架参考”，你要把里面的默认术语替换成最贴近原著的术语、能力边界与断层说明。',
        '',
        '【生成要求】',
        '- 先写“原著力量体系摘要”，再做“累计值 -> 原著术语/子阶段”的完整映射。',
        '- 若原著存在明确境界/等级/武力阶梯，优先沿用原著术语，并让当前累计值去适配原著，而不是让原著去适配默认术语。',
        '- 若原著只有模糊强弱层次，没有完整分段，就在不违背原著的前提下做最小补段，优先补成初期 / 中期 / 后期 / 圆满；高境不足时可收束为三段或单段。',
        '- 不要只写抽象高境界点评；开局常见层级、门派前辈层级、地方强者层级、顶尖高手层级都要能直接判定。',
        '- 除非原著本来就明确使用“几重”“几层”，否则不要把默认回退体系写成“第一境十重”这类过密小层级。',
        '- 输出必须覆盖数值映射、九阶命名与能力边界、境界差距口径、终点文案、阶段推进表、大境突破表、武侠硬边界。',
        '- 不要输出 \`【境界体系提示词】\` 或 \`【境界使用策略】\`；这两个固定头部由项目现有提示词文件提供。',
        '- 阶段推进和大境突破不能留空，也不能只写“同修炼体系协议一致”；你必须把完整表写出来。',
        '- 不要输出“见上文”“同前文”这类省略写法；本次输出必须自足可用。',
        '- 不要输出世界名称、主要势力、地图格局、社会秩序、主线事件梗概；这些属于世界观正文，不属于境界体系提示词。',
        '- 只输出 <境界体系>...</境界体系>。'
    ].join('\n');
};
