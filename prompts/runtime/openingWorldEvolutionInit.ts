export const 开局世界演变初始化附加提示词 = [
    '【开局世界演变初始化任务】',
    '- 当前任务是第0回合开局完成后的首次世界初始化，不是普通回合后的例行维护。',
    '- 本任务严禁输出 `<正文>`、`<短期记忆>`、`<行动选项>` 或任何主剧情叙事段落；职责只限于初始化后台世界内容。',
    '- 你的输出目标是让第1回合开始就能持续运转的后台世界结构落地，而不是再生成一段开场故事。',
    '- 主开局只负责第一幕正文与前台根路径初始化；你在这里负责接管并初始化 `世界` 根路径及必要的 `环境.*` 余波承接。',
    '- 先把开局正文、开局<剧情规划> 与已成立命令视为前台已发生事实，再据此建立能从第1回合开始持续运转的后台世界结构。',
    '- 本任务不是零碎补漏，而是把开局后必须马上开始运转的后台对象一次建稳：`活跃NPC列表 / 待执行事件 / 进行中事件 / 已结算事件 / 世界镜头规划 / 江湖史册 / 地图 / 建筑`。',
    '- 世界初始化默认只建立后台结构，不重写前台人物/环境真值，不重写 `剧情.*` 当前章结构，也不补第0回合正文的后续演出。',
    '- 主角本人不得写入 `世界.活跃NPC列表`；这里只初始化后台远端或场外仍需追踪的对象。',
    '- 当前就在主角眼前、已经进入第0回合现场互动、或本轮正文已经明确按前台处理的人物，默认不作为普通后台活跃 NPC 初始化；确需保留时，也只能按受阻/在场打断语义最小记录，而不是当作正常远端推进项。',
    '- 所有后台对象都要优先回答三件事：什么时候能动、为什么能动、动完会沉淀什么；没有时间门槛和条件门槛的空壳对象不要写。',
    '- 若补写 `世界.活跃NPC列表`，每条都必须补齐具体 `当前行动`；不能只给空状态，不给“此人此刻正在做什么”的当前动作；也不要把历史行动堆成数组。',
    '- 若补写 `世界.进行中事件`，按常态 5 条、峰值 7 条的后续维护口径初始化；只建立已经具备开始原因、当前进展和影响面的事件，不为凑数预埋空壳进行中事件。',
    '- `世界.进行中事件[].当前进展` 只写单条当前摘要，不写历史日志数组；如果要概括开局前后推进结果，先吸收旧信息，再直接写成当前摘要。',
    '- 禁止与开局前台命令重复写同义世界/环境命令；世界初始化要补的是后台结构，不是把正文已写明的前台状态再抄一遍。',
    '- 本任务不负责 `剧情.*`、女主规划树、当前章任务树与前台社交补丁；这些由开局后的规划初始化链路负责，不在这里越权落地。',
    '- 局部命令只允许落在已存在且结构完整的对象或数组项上；若对象不存在，先整对象 `set`；若数组项不存在，先整项 `push`。',
    '- 禁止对空对象、缺失数组项或半结构对象直接写 `set 子字段`、`push 子数组`、`add 子字段`。',
    '- 命令顺序固定为“先 `set/add`，再 `push`，最后 `delete`”；若同一数组存在多个 `delete`，按索引从大到小输出。',
    '- 开局初始化阶段不得为了凑世界演变而强行结算到期项；除非开局文本与命令已经明确给出到期节点，否则`到期触发`默认按“无”处理。',
    '- 若本回合是同人开局，必须详细逐项对照附加的小说分解滑窗，尤其是 `【当前章节内容】` 与 `【下一章节内容】`：按 `承接事实 / 本组结束状态 / 关键事件 / 角色推进 / 原著硬约束 / 可提前铺垫 / 时间线范围` 初始化后台世界，先贴原著时序与原著世界逻辑，再决定哪里只做预热、哪里可以入场。',
    '- 同人开局初始化活跃 NPC 时，优先使用原著已存在人物、原著势力链可合理外延人物、以及已被开局偏转事实明确生出的人物；原著角色的后台动作要贴合当前章节窗口与原著时期。',
    '- 同人开局只允许把当前章已经成立、或已经逼近到可预热的后台世界状态写进去；下一章只写预热、探查、调度、气压累积与条件逼近，不提前写成已发生结果。',
    '- 若开局正文与现有状态还不足以证明某条后台线已经开始运转，就保持空、留到后续世界维护再建，不为了“后台看起来热闹”硬塞空壳 NPC、空壳事件或空壳镜头。',
    '',
    '【执行顺序】',
    '- 第一步先拆开前台与后台：把开局正文、开局命令、开局<剧情规划>中已经前台坐实的事实，与真正需要后台持续追踪的对象分开。',
    '- 第二步先确定后台最小骨架：哪些远端人物需要进 `活跃NPC列表`，哪些事件应放进 `待执行事件` 或 `进行中事件`，哪些镜头应放进 `世界镜头规划`。',
    '- 第三步再补江湖史册、地图、建筑等长期承接结构；这些只记录真正有后续导航或长期世界价值的对象，不为凑结构乱建节点。',
    '- 第四步做时间门槛、条件门槛与同人章节边界校验；只让已经具备开始理由的后台线入场。',
    '- 第五步做命令合法性复核：确保不重复前台命令、不越权写剧情树、不把下一章结果或未开始事项提前写成后台既成事实。'
].join('\n');

export const 构建开局世界演变初始化上下文 = (params: {
    openingBodyText?: string;
    openingPlanText?: string;
    openingCommandTexts?: string[];
    currentGameTime?: string;
}): string => [
    '【开局世界演变初始化上下文】',
    `- 当前游戏内时间：${(params.currentGameTime || '').trim() || '未知时间'}`,
    '- 这是开局生成后的首次后台世界初始化；若需要补命令，应优先建立可持续运转的后台结构，而不是重复前台事实。',
    '',
    '【开局正文】',
    (params.openingBodyText || '').trim() || '暂无',
    '',
    '【开局规划】',
    (params.openingPlanText || '').trim() || '无',
    '',
    '【开局已成立命令】',
    (Array.isArray(params.openingCommandTexts) ? params.openingCommandTexts : []).length > 0
        ? (params.openingCommandTexts || []).join('\n')
        : '无',
    '',
    '任务：根据第0回合已落成的第一幕与当前状态，初始化开局后的后台世界，让动态世界独立API从第1回合开始就拥有可持续演变的世界锚点。禁止续写正文；只做世界初始化与最小补丁。',
    '提醒：这里只处理后台世界结构，不重复前台命令，不重写 `剧情.*` 规划树，不补第0回合第二幕。'
].join('\n');
