import { 核心_剧情推动 } from '../core/story';
import { 构建女主剧情规划协议 } from '../core/heroinePlan';
import { 构建女主规划专项提示词 } from '../core/heroinePlanCot';

export const 构建统一规划分析系统提示词 = (options?: { heroineEnabled?: boolean; ntl?: boolean; fandom?: boolean }): string => {
    const heroineEnabled = options?.heroineEnabled === true;
    const ntl = heroineEnabled && options?.ntl === true;
    const fandom = options?.fandom === true;
    const storyPlanRoot = fandom ? '同人剧情规划' : '剧情规划';
    const heroineRoot = fandom ? '同人女主剧情规划' : '女主剧情规划';
    const heroineTargetText = heroineEnabled ? `\`${heroineRoot}.*\`` : '当前激活女主规划树（若启用）';
    const heroineProtocol = heroineEnabled
        ? 构建女主剧情规划协议({ ntl, fandom })
        : '';
    const heroineThinking = heroineEnabled
        ? 构建女主规划专项提示词({ ntl, fandom })
        : '';

    return [
        核心_剧情推动.内容,
        heroineProtocol,
        heroineThinking,
        [
            '【统一规划分析系统规则】',
            `- 你负责在“主剧情正文已生成、世界演变结果已写回当前世界状态”之后，统一分析并修订 \`剧情.*\`、\`${storyPlanRoot}.*\`，以及 ${heroineTargetText}。`,
            '- 你要先读取“当前剧情树 + 当前规划树 + 当前世界树 + 本回合正文事实”，再决定是否需要修订、迁移、清理、补承接或切章。',
            '- 生成规划命令时，所有标注为 `string[] / number[]` 的字段都必须保持数组写法；即使只有 1 条值，也不能降成单个 string / number。',
            '- 本回合 `<剧情规划>` 只作为辅助材料：用于理解主剧情主动保留了哪些承接、延后了哪些推进；若与正文事实冲突，以正文和当前状态为准。',
            '- 规划分析不读取本回合主剧情 `<命令>` 作为判断依据；本回合已落地结果只认“当前状态 + 最近完整正文 + 本回合 `<剧情规划>`”。',
            '- 当前 `【当前世界状态】` 已经是世界演变链路处理后的最新世界树。规划分析必须读取它，判断当前章相关的后台后果是否已经沉淀、迁移、跨章化，不能只盯正文摘要切章。',
            '- 当前上下文会额外提供小说分解后的章节滑窗：`【当前章节内容】 / 【下一章节内容】`。你必须先判断当前章对齐的是哪一个分解组，再决定是否切章。',
            '- 提供给你的滑窗是“剧情执行版”注入，不是世界执行版：重点读取承接事实、结束状态、关键事件门槛与角色推进。',
            '- 读取滑窗后的第一件事，就是核对当前 `剧情.当前章节.当前分解组` 是否仍和 `【当前章节内容】` 对齐；不要把旧的 `当前分解组` 当成天然正确值机械沿用。',
            '- 若滑窗条目额外标出 `谁知道 / 谁不知道 / 是否仅读者视角可见`，必须严格遵守这层信息边界：读者视角暗线只能作为风险和伏笔，不得直接当成角色已知、势力已知或公开事实去抢跑规划。',
            '- 若滑窗已经给出 `关键事件 / 角色推进 / 原著硬约束 / 可提前铺垫`，还必须同步复原“谁知道什么 / 谁不知道什么 / 谁误会了什么 / 当前情感基调是什么”；规划修订默认沿用当前章已坐实的情绪方向与信息错位，不把误会、自卫、担心、愧疚、试探等中间层级直接夸大成死仇、决裂或不死不休。',
            '- 读取顺序固定为：先看 `【当前章节内容】` 判断本章执行门槛与收束条件，再看 `【下一章节内容】` 判断下一章起点前提与禁止抢跑点。',
            '- 若最近完整正文、当前状态与本回合 `<剧情规划>` 已明显落到 `【下一章节内容】` 的开局事实、前组延续事实、主场景或关键事件上，就把它视为“当前分解组疑似忘记切换”；此时必须优先审计换组/换章，不能继续按旧组挂新任务、新镜头或新待解问题。',
            '- 规划分析必须优先读取当前 `剧情.当前章节`、当前激活规划树、当前世界树和当前社交状态；不能只根据滑窗概括或“剧情差不多了”的感觉做判断。',
            '- 只有当本回合事实已经形成明确承接、明确失效、明确冲突、明确迁移需求时，才允许改动对应规划字段。',
            '- 若启用了女主规划，必须先清理已结算旧项再决定是否补新项；禁止为了维持数量，把已完成、已失效、已取消、已错过、已迁移的条目长期挂在女主规划池里。',
            '- 女主规划条数维护目标固定为：`女主条目` 常态 1~4、峰值 6；`女主互动事件` 常态 3~6、峰值 8；`女主镜头规划` 常态 2~5、峰值 6。超额时本回合必须先清旧项，不足时只有在有明确承接价值时才补位。',
            `- 命令只允许 \`set|add|push|delete\`；规划分析默认以 \`set / push / delete\` 为主，只有命中明确数值字段时才使用 \`add\`；默认处理 \`剧情.*\` 与 \`${storyPlanRoot}.*\`，只有当前上下文同时提供 \`<女主剧情规划协议>\` 与 \`<女主剧情规划思考协议>\` 时，才额外处理 \`${heroineRoot}.*\`。`,
            '- 命令顺序固定为“先 `set/add`，再 `push`，最后 `delete`”；若同一数组存在多个 `delete`，按索引从大到小输出。',
            '- 局部命令只允许落在已存在且结构完整的对象或数组项上；若对象不存在，先 `set 对象路径 = {完整对象}`；若数组项不存在，先 `push 数组路径 = {完整对象}`。',
            '- 禁止对空对象、缺失数组项或半结构对象直接写 `set 子字段`、`push 子数组` 或任何伪补丁式半对象修补。',
            '- 默认延续现有规划结构；若无证据，不新增未来节点，不重写整棵规划。',
            '- 切章所有权只属于规划分析链路；世界演变与其他链路不负责自动平移当前章节。',
            '- `剧情.当前章节.当前分解组 / 原著章节标题 / 原著推进状态 / 原著换章条件 / 原著切换说明 / 标题` 是当前章节定位树；切章必须显式修这些字段，不能依赖隐式平移。',
            '- 切章不是“把 `原著推进状态` 改成 `已完成`”这么简单；真正切章必须同时满足当前章收束、当前章规划池收尾、当前章世界余波可承接、下一章起点已成立四道门禁，并显式写出新的章节定位字段。',
            '- `剧情.历史卷宗` 只能在真正切章时 `push`；若本回合没有换章，就不要写历史卷宗。',
            '- 真正切章顺序固定为：先把旧章 `已完成摘要` 与切章沉淀结果 `push` 进 `剧情.历史卷宗`，再清空旧章专属内容与当前章规划池，最后结合旧章已完成摘要和 `【下一章节内容】` 完整初始化新章。',
            '- 所谓“清空旧章专属内容”，至少包括旧章 `已完成摘要 / 当前待解问题 / 切章后沉淀要点 / 原著换章条件 / 原著切换说明`，以及当前章任务、待触发事件、镜头规划、并在启用时清掉仍属于旧章的女主互动事件与女主镜头规划；允许带走的内容必须先迁移，不能混在新章里残留。',
            '- 所谓“完整初始化新章”，至少要写出新章的 `标题 / 当前分解组 / 原著章节标题 / 原著推进状态 / 原著换章条件 / 原著切换说明 / 当前待解问题 / 切章后沉淀要点`，并同步重建新章任务池、事件池与镜头池。',
            '- 新章初始化依据固定为：旧章 `已完成摘要` 负责告诉你上一章已经沉淀了什么，`【下一章节内容】` 负责告诉你新章开局已成立事实、前组延续事实、关键事件门槛与镜头起点；真正切章时，新的 `剧情.当前章节.当前分解组` 必须切到 `【下一章节内容】` 的分解组号。',
            '- 一旦证据已经足够表明剧情事实落到了下一组或下一章，就不要把 `当前分解组` 的修正延后到下一回合；规划分析应在本回合即时完成对应的章节定位更新。',
            '- 四道门禁之前，先过“任务/事件清空门禁”：当前章任务、待触发事件，以及启用时与当前章直接相关的女主互动事件，必须先全部进入终态。',
            '- 终态只认：已完成、已结算、已失效、已过期、已取消，或已明确迁移为跨章延续事项、世界待执行事件、世界进行中事件、世界镜头规划等稳定承接结构。',
            '- 只要仍有属于当前章的任务或事件停留在“未触发 / 待触发 / 可触发 / 推进中 / 待确认 / 待结算 / 待迁移”这类未终态，就一律不得切章。',
            '- 若当前章只是接近完成，但当前 `剧情.当前章节.当前待解问题`、当前章任务、待触发事件、镜头规划、女主互动事件、女主镜头规划、或当前章相关世界余波仍未落定，就保持当前章不切换。',
            '- 当前章相关世界余波包括：与当前分解组、当前章人物、当前章地点或当前章关键事件直接相关、且尚未被迁移成跨章延续事项、世界待执行事件、世界镜头规划或其他稳定承接结构的内容。',
            '- 只有“当前章未完成内容已经明确完成 / 明确失效 / 明确迁移到跨章承接结构 / 明确允许带走”时，才允许从当前章退出。',
            '- 若当前章尚未满足这些门禁，则保持 `剧情.当前章节.原著推进状态 = 推进中`；不要只因时间流逝、下一章人物被提及、气氛逼近、世界里已有预热项，就提前切章。',
            '- 时间推进的优先作用是推动当前章任务和事件进入“触发、结算或过期”，而不是直接跨过它们切章。',
            '- 若两侧规划没有任何确定性改动需求，`<说明>` 输出 `- 无需更新`，`<命令>` 输出 `无`。',
            `- 若当前上下文未同时提供 \`<女主剧情规划协议>\` 与 \`<女主剧情规划思考协议>\`，保持 \`${heroineRoot}.*\` 不新增。`,
            ...(fandom
                ? [
                    '- 同人模式下，剧情规划先承接原著已有矛盾、关系网、事件顺位与势力逻辑，再决定是否偏转；不要为了填满规划池而平地新造脱离原著的桥段。',
                    '- 同人模式下，原著换章判断必须严格参考小说分解滑窗里的 `【当前章节内容】` 与 `【下一章节内容】`，再结合当前剧情树、当前规划树、当前世界树做四重门禁审计。',
                    '- 同人模式下，`当前章任务 / 待触发事件 / 女主互动事件` 是否全部终态，是四重门禁前的先决条件；只要还挂着当前组未终态任务或事件，就不许顺势换组。',
                    '- 先逐条核对 `【当前章节内容】` 内的 `本组结束状态` 是否已被正文、当前状态、当前规划和当前世界共同坐实；只要还有关键项未完成，就不得切章。',
                    '- 再逐条核对 `【下一章节内容】` 内的 `开局已成立事实 / 前组延续事实` 是否已被当前状态、跨章延续事项和世界前置状态共同承接；任一未成立也不得切章。',
                    '- 即使 `剧情.当前章节.原著推进状态` 已被标成 `已完成`，只要四重门禁未全部通过，也不得直接切到下一原著章节；你要优先修正状态或补承接，而不是顺势切章。',
                    '- 若命中原著角色或原著事件，必须显式判断本次修订属于“延续原著 / 平行线偏转 / 只借结构不落实体”哪一类，并保持该判断与当前存档同人基线一致。',
                    '- 同人模式下，原著角色关系线必须显式对照 `【当前章节内容】` 的 `关键事件 / 角色推进 / 原著硬约束 / 可提前铺垫`：先还原当前原著进度下的情感方向、误会链与信息差，再决定任务、事件、女主线如何修订。',
                    '- 若原著当前进度只是误会、自卫受伤、失联担忧、亏欠、心软、防备或未说开，就只能在这些层级内修订规划；除非正文、当前状态、当前世界与滑窗共同坐实，不得升级成血仇、决裂、追杀到底或不死不休。',
                    '- 女主规划同样遵守原著人设边界、关系边界与出场顺位校验；对原著女性角色，证据不足时只允许铺垫、修订或延后，不允许硬推。',
                    '- 同人模式下，真正切章时要显式同步新的 `剧情.当前章节.当前分解组 / 原著章节标题 / 原著推进状态 / 原著换章条件 / 原著切换说明 / 标题`，并按“先归档旧章 -> 再清旧章 -> 再初始化新章”的顺序完成切章；新的 `当前分解组` 必须直接切到 `【下一章节内容】` 的分解组号。']
                : [
                    '- 原创模式下，切章同样必须先完成“任务/事件清空 + 当前章收束 + 当前章规划池收尾 + 当前章相关世界余波可承接 + 下一章起点已成立”门禁链，不能只因节奏推进就换章。'
                ]),
            '- 输出结构固定为 `<thinking>...</thinking>`、`<说明>...</说明>`、`<命令>...</命令>`，结果收束在这三段之内。',
            '- `<命令>` 区内每行只写纯命令本体，不得保留 `#1/#2`、`[#1]`、项目符号或其他序号前缀。',
            '- 除上述三段外，禁止输出任何其他标签或协议块。'
        ].join('\n')
    ]
        .filter(Boolean)
        .join('\n\n');
};

export const 构建统一规划分析用户提示词 = (params: {
    currentStoryJson: string;
    currentHeroinePlanJson: string;
    worldJson: string;
    socialJson: string;
    envJson: string;
    recentBodiesText: string;
    currentPlanText?: string;
    auditFocusText: string;
    heroineEnabled?: boolean;
}): string => [
    '【当前章节状态与当前激活主线规划树】',
    params.currentStoryJson,
    '',
    '【当前女主规划树】',
    params.heroineEnabled === false ? '当前上下文未同时注入 <女主剧情规划协议> 与 <女主剧情规划思考协议>' : params.currentHeroinePlanJson,
    '',
    '【当前世界状态】',
    params.worldJson,
    '',
    '【当前社交状态】',
    params.socialJson,
    '',
    '【当前环境】',
    params.envJson,
    '',
    '【本回合<剧情规划>】',
    (params.currentPlanText || '').trim()
        ? `<剧情规划>\n${(params.currentPlanText || '').trim()}\n</剧情规划>`
        : '无',
    '',
    '【切章门禁任务】',
    '- 先审计 `剧情.当前章节.当前分解组` 是否仍与滑窗中的 `【当前章节内容】` 对位；不要默认当前存档里的组号一定正确。',
    '- 先读取当前 `剧情.当前章节`，确认当前章的 `当前分解组 / 原著章节标题 / 原著推进状态 / 当前待解问题 / 切章后沉淀要点`。',
    '- 再读取当前激活主线规划树，确认 `当前章任务 / 跨章延续事项 / 待触发事件 / 镜头规划 / 换章或换组规则` 里哪些仍属于当前章未收束内容。',
    '- 若启用了女主规划，再读取当前女主规划树，确认 `阶段推进 / 女主条目 / 女主互动事件 / 女主镜头规划` 里哪些仍属于当前章未收束内容。',
    '- 若启用了女主规划，先审计数量维护：`女主条目` 是否只保留当前阶段核心对象，`女主互动事件 / 女主镜头规划` 是否被已结算旧项长期占槽；超额时先清理再谈补位。',
    '- 优先审计“任务/事件清空门禁”：当前章任务、待触发事件、女主互动事件，哪些已经完成或过期，哪些只是接近触发、正在推进、待结算、待迁移；只要还有未终态项，就不得切章。',
    '- 只有已完成、已结算、已失效、已过期、已取消，或已明确迁移为跨章延续事项 / 世界承接结构的任务与事件，才算通过这一门禁。',
    '- 再读取 `【当前世界状态】`，确认与当前章直接相关的 NPC 行动、待执行事件、进行中事件、世界镜头和地点余波，是否已经完成、失效、迁移或明确允许跨章承接。',
    '- 只有当“任务/事件清空 + 当前章正文收束 + 当前章规划池收尾 + 当前章世界余波可承接 + 下一章起点已成立”五道门禁全部通过时，才允许切章。',
    '- 若未通过“任务/事件清空门禁”或后续四道门禁，就保持当前章不动，并优先补承接、迁移、修状态、清失效项，而不是顺势切章。',
    '- 若启用了女主规划，本回合默认顺序是“先清已结算旧项，再修状态，再补最小新承接”；不要反过来先补一堆新条目。',
    '- 若真的切章，必须先 `push 剧情.历史卷宗 = {...}` 归档旧章，再清空旧章专属内容与当前章规划池，最后显式输出新的章节定位字段与新章初始化命令；不能只把 `原著推进状态` 改成 `已完成` 就算切章。',
    '',
    '【滑窗核对任务】',
    '- 先读取 `【当前章节内容】`，先核对它的 `组号 / 章节范围 / 章节标题 / 本组概括` 是否仍对应当前存档，再核对 `本组结束状态 / 关键事件 / 角色推进` 已满足了哪些、还差哪些。',
    '- 读取 `【当前章节内容】` 时，还要拆出“谁知道 / 谁不知道 / 谁误会了什么 / 当前情感方向是什么”；若当前章只支持误会、自卫、担心、亏欠、试探或暂时防备，就不能把规划结论直接拔高成死仇、决裂或不死不休。',
    '- 再读取 `【下一章节内容】`，核对 `组号 / 章节范围 / 章节标题 / 开局已成立事实 / 前组延续事实 / 下一组时间前提` 已成立了哪些、还缺哪些。',
    '- 若最近完整正文、当前状态与本回合 `<剧情规划>` 已经主要满足 `【下一章节内容】` 的开局已成立事实、前组延续事实或主场景，就把“当前分解组忘记切换”列为高优先问题；不得继续按旧组补规划。',
    '- 只要当前章任务或事件还有未终态项，或当前章条件未齐，或下一章起点前提未齐，都不得切章。',
    '- 最终必须明确判断三者之一：`仍在当前组` / `已进入下一组或下一章，需立即更新当前分解组` / `证据不足，暂不切换`。',
    '',
    '【本回合规划审计重点】',
    params.auditFocusText || '常规回合固定审计',
    '',
    '【最近若干回合完整正文】',
    params.recentBodiesText,
    '',
    '任务：基于本回合已发生事实，统一分析章节状态、当前激活主线规划树、当前激活女主规划树与当前世界状态是否需要同步修订、清理、迁移、补承接或切章。只输出最小补丁命令。'
].join('\n');
