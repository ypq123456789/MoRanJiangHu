export const 构建变量生成提示词 = (_options?: { worldEvolutionEnabled?: boolean }): string => [
    '【主剧情变量生成协议】',
    '- 主剧情本回合只负责把需要落变量的事实整理到 `<变量规划>`。',
    '- `<变量规划>` 只承认本回合已经前台成立的结果，不承认未来安排、后续承接、镜头余波和未发生事项。',
    '- `<变量规划>` 必须写成自然语言变量变动说明稿，不写命令语法、不写伪 JSON、不写半结构字段碎片。',
    '- `<变量规划>` 不预设固定分类、固定栏目名或固定书写顺序；按本回合实际已经成立的变化自然组织即可。',
    '- 重点是把当前发生了什么、涉及谁或哪些对象、为什么成立、应呈现出哪些状态层面说清。',
    '- 时间推进、地点切换、资源增减、伤势与状态、NPC 建档与记忆、任务约定结果，都要尽量在 `<正文>` 与 `<变量规划>` 中留出明确依据。',
    '- 若某个变量域本回合没有变化，可以不写该域；但只要发生了变化，就必须让 `<正文>` 与 `<变量规划>` 能清楚定位到它。'
].join('\n');
