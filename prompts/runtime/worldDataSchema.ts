export const 世界变量结构参考提示词 = `
<世界变量结构参考>
# 世界独立数据结构参考

## 0. 使用方式
- 当前世界演变只认这些根路径：\`世界\` 与必要时联动的 \`环境\`。
- 路径使用无前缀写法；命令统一从 \`世界\` 顶层起写。
- 这里只定义世界层字段树与元素结构，不负责命令顺序与输出格式。
- 运行时注入给模型时，数组会保留原形；若数组元素是对象，阅读视图中通常会额外带一行 \`"[0]"\` / \`"[1]"\` 这类锚点，仅用于阅读定位。
- 更新已有对象时，只有在目标对象已存在且结构完整时，才使用 \`set 世界.xxx[i].字段\` 精准刷新。
- 新增数组项时，优先使用 \`push 世界.xxx = {...}\`，并一次写完整对象。
- 若 \`世界.xxx[i]\` 尚不存在，不要直接 \`set 世界.xxx[i].字段\`；先 \`push 世界.xxx = {完整对象}\` 建项，再做后续局部修改。
- 若需要联动环境时间，只写 \`set 环境.时间 = "YYYY:MM:DD:HH:MM"\`，不要拆开写年月日时分。
- \`沉淀内容[] / 归档内容[]\` 这类累计数组，若只是新增单条事实，优先用 \`push\` 追加，不要整组 \`set\` 覆盖。
- \`世界.进行中事件[i].当前进展\` 是单个“当前摘要”字符串，不是历史日志数组；每轮若有变化，应综合旧进展后用 \`set\` 覆写成最新摘要。
- 字段暂缺时，可保留空字符串、空数组或 null；但与时间、条件、状态直接相关的核心字段不要留空。
- 命令保持最小路径：只改哪个字段，就写哪个字段。
- \`世界.进行中事件\` 普通条数常态建议维持 5 条、峰值不超过 7 条；不足时优先承接既有待执行与旧线发酵，超过峰值时要在当回合清回阈值内。
- \`世界.已结算事件\` 是“近期结算缓冲层”，不是长期仓库；普通条数常态建议维持 4 条、峰值不超过 6 条，超额时要做定量归档或删除。

## 1. 世界演变可写根树
世界
├─ 活跃NPC列表
├─ 待执行事件
├─ 进行中事件
├─ 已结算事件
├─ 世界镜头规划
├─ 江湖史册
├─ 地图
└─ 建筑

## 2. 世界
世界
├─ 活跃NPC列表: Array<世界.活跃NPC列表[i]>
├─ 待执行事件: Array<世界.待执行事件[i]>
├─ 进行中事件: Array<世界.进行中事件[i]>
├─ 已结算事件: Array<世界.已结算事件[i]>
├─ 世界镜头规划: Array<世界.世界镜头规划[i]>
├─ 江湖史册: Array<世界.江湖史册[i]>
├─ 地图: Array<世界.地图[i]>
└─ 建筑: Array<世界.建筑[i]>

### 2.1 【类型别名】世界.活跃NPC列表[i]
世界.活跃NPC列表[i] = {
  姓名:string,
  所属势力:string,
  当前位置:string,
  当前状态:string,
  当前行动:string,
  行动开始时间:string(YYYY:MM:DD:HH:MM),
  行动结束时间:string(YYYY:MM:DD:HH:MM)
}

### 2.2 【类型别名】世界.待执行事件[i]
世界.待执行事件[i] = {
  事件名:string,
  类型:string,
  事件说明:string,
  计划执行时间:string(YYYY:MM:DD:HH:MM),
  最早执行时间:string(YYYY:MM:DD:HH:MM),
  最晚执行时间:string(YYYY:MM:DD:HH:MM),
  前置条件:string[],
  触发条件:string[],
  阻断条件:string[],
  执行后影响:string[],
  错过后影响:string[],
  关联人物:string[],
  关联势力:string[],
  关联地点:string[],
  关联分解组:number[],
  关联分歧线:string[],
  当前状态:string
}

### 2.3 【类型别名】世界.进行中事件[i]
世界.进行中事件[i] = {
  事件名:string,
  类型:string,
  事件说明:string,
  开始时间:string(YYYY:MM:DD:HH:MM),
  预计结束时间:string(YYYY:MM:DD:HH:MM),
  当前进展:string,
  已产生影响:string[],
  关联人物:string[],
  关联势力:string[],
  关联地点:string[],
  关联分解组:number[],
  关联分歧线:string[]
}

### 2.4 【类型别名】世界.已结算事件[i]
世界.已结算事件[i] = {
  事件名:string,
  类型:string,
  事件说明:string,
  结算时间:string(YYYY:MM:DD:HH:MM),
  事件结果:string[],
  长期影响:string[],
  是否进入史册:boolean,
  关联人物:string[],
  关联势力:string[],
  关联地点:string[],
  关联分解组:number[],
  关联分歧线:string[]
}

### 2.5 【类型别名】世界.世界镜头规划[i]
世界.世界镜头规划[i] = {
  镜头标题:string,
  镜头内容:string,
  触发时间:string(YYYY:MM:DD:HH:MM),
  触发条件:string[],
  关联人物:string[],
  关联地点:string[],
  关联分解组:number[],
  关联分歧线:string[],
  沉淀内容:string[],
  当前状态:string
}

### 2.6 【类型别名】世界.江湖史册[i]
世界.江湖史册[i] = {
  标题:string,
  归档时间:string(YYYY:MM:DD:HH:MM),
  归档内容:string[],
  长期影响:string[],
  关联人物:string[],
  关联势力:string[],
  关联地点:string[],
  关联分歧线:string[]
}

### 2.7 【类型别名】世界.地图[i] / 世界.建筑[i]
世界.地图[i] = {
  名称:string,
  坐标:string,
  描述:string,
  归属:{ 大地点:string, 中地点:string, 小地点:string },
  内部建筑:string[]
}
世界.建筑[i] = {
  名称:string,
  描述:string,
  归属:{ 大地点:string, 中地点:string, 小地点:string }
}

## 3. 写入口径
- \`活跃NPC列表\` 记录的是后台可执行行动，只追踪当前不在场、仍需后台模拟的对象；主角本人不得写入 \`世界.活跃NPC列表\`。
- \`活跃NPC列表\` 只收“已经开始执行”的后台行动项；尚未开始、仍在等条件、仍在预热的线索，继续留在 \`待执行事件\`、\`世界镜头规划\` 或其他未来池，不要提前 push 进来。
- \`当前行动\` 只写该 NPC 此刻正在执行的那一件事，使用单条自然语言描述；不要把历史行动、阶段回顾、未来计划堆成数组。
- \`行动开始时间 / 行动结束时间\` 是这条后台行动的闭合时间段：前者表示已经进入执行态的起点，后者表示预计何时该结算、续段、改道或迁移。
- 若该 NPC 本回合已经进入在场互动、当前就在主角眼前、或后台动作被台前剧情截住，就不要继续把它当正常后台推进项；应把该条 \`set\` 成 \`当前状态="[打断]当前在场"\` 一类可追溯状态。字面量 \`[打断]当前在场\` 只属于 \`当前状态\` 字段，禁止写进 \`当前行动\`；\`当前行动\` 只保留被打断前的后台动作说明或当前的受阻描述。
- 该 NPC 离场后若仍需继续后台跟踪，先删除旧的打断项，再重新 \`push 世界.活跃NPC列表 = {完整对象}\` 建立新一轮后台行动，不直接沿用旧打断项续写。
- 同人模式下，\`活跃NPC列表\` 优先刷新原著已存在人物、原著势力链可合理外延人物、以及已被当前偏转事实明确生出的人物；原著角色行动要贴合时期、立场、情报能力、行动半径与章节窗口。
- \`待执行事件\` 负责“尚未开始但已具备时间门槛与条件门槛”的事件；\`进行中事件\` 负责已开始推进的事件；\`已结算事件\` 负责已落定结果。
- \`进行中事件[i].当前进展\` 只写“事件此刻推进到哪一步”的单条摘要；新变化应先吸收旧摘要，再 \`set\` 成新的当前进展，不把旧进展一条条遗留在变量里。
- \`已结算事件\` 只保留近期仍需观察余波、等待主线消化、或等待判断是否入史册的事件；结果一旦沉淀完成，就应归档或删除。
- \`世界镜头规划\` 负责展示镜头外正在发生什么、哪些氛围和压力正在逼近；它不是可删字段。
- \`江湖史册\` 只记录“已经落定且会影响后续长期走向”的历史归档，不记录普通短时波动。
- \`已结算事件\` 的清理顺序固定为：先迁 \`是否进入史册=true\` 且长期影响已成立的项，再删结果已沉淀的普通项，最后若仍超额，再删最旧、最弱、与当前章最远的项。
- 新增活跃 NPC 时，优先把 \`当前行动 / 行动开始时间 / 行动结束时间 / 当前状态\` 写完整；新增待执行事件时，再去补齐前置条件、触发条件、阻断条件与时间门槛。
- \`世界\` 中不再放 \`势力常识\`、\`地理常识\` 这类世界观提示词字段。
- 若新增新的可独立抵达地点，优先写入 \`世界.地图\`；只有地点内部的房间、院落、殿阁、后厨、地牢等微观构造，才优先写入 \`世界.建筑\`。
- \`世界.地图[i].坐标\` 固定为 \`[x,y]\` 字符串。
</世界变量结构参考>
`;
