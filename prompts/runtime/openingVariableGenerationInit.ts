import { 构建修炼体系附加块 } from '../../utils/promptFeatureToggles';

const 开局变量初始化覆盖清单 = [
    '【开局变量初始化覆盖清单】',
    '- 环境：时间、大地点、中地点、小地点、具体地点、天气、节日、环境变量。',
    '- 角色：基础档案、六维、金钱、精力/饱腹/口渴/负重、坐标、七部位血量与状态、装备、物品、BUFF。',
    构建修炼体系附加块('- 角色：若启用修炼体系，再覆盖境界/境界层级、经验、内力、功法、突破条件。'),
    '- 社交：本回合已经正式在前台成立、且第1回合立刻会用到的 NPC 最小完整档案、关系状态、记忆、是否在场等。',
    '- 战斗：是否战斗中、敌方列表、必要的轮次/阶段/战场效果。',
    '- 玩家门派：基础信息、玩家职位/贡献、任务列表、兑换列表、重要成员。',
    '- 任务列表：已正式存在的任务对象、目标列表、状态、奖励描述。',
    '- 约定列表：已正式存在的约定对象、对象/地点/时间/状态/后果。'
].join('\n');

export const 开局变量生成附加提示词 = [
    '【开局变量生成任务】',
    '- 当前任务是第0回合开局完成后的首轮前台变量生成，不是普通回合的回合后补录，也不是增量补丁。',
    '- 你要根据第0回合正文与开局变量规划，把第1回合立刻要用到的前台变量树一次落稳，让后续独立世界/规划链路直接建立在这份前台状态上。',
    '- 将第0回合 `<变量规划>` 视为自然语言的完整前台初始化说明稿，而不是增量摘要；优先按其中写明的当前终态、依据与对象结构生成最终变量命令。',
    '- 开局变量生成属于“首回合全域初始化审计”：必须逐域复核 `环境`、`角色`、`社交`、`战斗`、`玩家门派`、`任务列表`、`约定列表`，不能因为 `<变量规划>` 某域写得少就直接跳过该域。',
    '- 即使 `<变量规划>` 漏写部分字段，也要结合正文、建档承接结果、当前变量树与当前空壳状态，补齐第1回合最小完整可用结构。',
    '- 变量生成完成后，后续独立链路才会继续做世界初始化与规划初始化；因此你要优先把前台根路径的时间、地点、人物、资源、关系、任务与约定状态写准。',
    '- 本任务只处理正式变量根域：`环境`、`角色`、`社交`、`战斗`、`玩家门派`、`任务列表`、`约定列表`。',
    '- 只承认第0回合正文里已经前台成立的事实；背景设定、未来安排、章节后续与后台世界余波都不提前写成前台变量。',
    '- 主角建档里已经明确给出的 `姓名`、`性别`、`年龄`、`外貌`、`性格`、`称号`、`天赋列表`、`出身背景`、`所属门派ID` 等，只要第0回合把它们承接成当前出场身份，就必须完整落入 `角色` 根域，不能因为它们来自建档就漏写。',
    构建修炼体系附加块('- 若建档已明确初始境界等修炼真值，只要第0回合把它们承接成当前出场身份，也必须完整落入 `角色` 根域。'),
    '- 若开局正文已经让某些状态自然成立，就直接落成变量；若证据不足，就保持最小、保守、可承接的当前值。',
    '- 若第0回合已正式出场或建档的 NPC 命中“女性 + 主要角色 + 高颜值明确”，必须直接落成完整档案，不留 `未知 / 不详 / 待补充 / 普通 / 正常` 这类占位字段。',
    '',
    开局变量初始化覆盖清单
].join('\n');

export const 构建开局变量生成承接提示 = (params: {
    currentGameTime?: string;
    openingRoleSetupText?: string;
    openingConfigText?: string;
}): string => [
    '【开局承接提示】',
    '- 当前是第0回合后的首轮变量生成；要把第1回合会读取的前台变量树逐域初始化到最小完整可用状态，不把它当普通补丁。',
    `- 当前游戏内时间参考：${(params.currentGameTime || '').trim() || '未知时间'}`,
    '- 这是开局正文完成后的第一次前台变量落地，后续世界初始化与规划初始化都会以这里产出的前台状态为基底。',
    '- 这里的 `<变量规划>` 是自然语言初始化说明稿；若它对某个根域写得不够细，不代表该根域可以不审计。',
    '',
    '【主角建档承接信息】',
    (params.openingRoleSetupText || '').trim() || '未提供显式主角建档信息。',
    '',
    '【本次开局配置】',
    (params.openingConfigText || '').trim() || '未提供额外开局配置。'
].join('\n');

export const 构建开局变量生成正文上下文 = (params: {
    openingBodyText?: string;
    openingVariablePlanText?: string;
    currentGameTime?: string;
    openingRoleSetupText?: string;
    openingConfigText?: string;
}): string => [
    '【第0回合开局变量生成上下文】',
    构建开局变量生成承接提示(params),
    '',
    '【第0回合完整正文】',
    (params.openingBodyText || '').trim() || '暂无',
    '',
    '【第0回合完整变量规划】',
    (params.openingVariablePlanText || '').trim() || '未提供显式变量规划，请完全依据正文推导。'
].join('\n');

export const 构建开局变量生成审计重点 = (params?: {
    fandomEnabled?: boolean;
}): string => [
    '【开局变量生成审计重点】',
    '- 先把 `<变量规划>` 读成“自然语言终态说明稿”：优先抓取当前终态、成立依据、新增对象或字段、本回合已发生变化，再回正文补足过程证据。',
    '- 然后按 7 大根域逐域审计：就算 `<变量规划>` 某域写得很短，也要判断该域是否需要首回合最小完整初始化。',
    '- 开局首回合优先保证：环境时间与地点可用、角色即时数值可用、在场 NPC 最小档案可用、门派/任务/约定引用可用，让第1回合能直接承接。',
    '- 角色根域必须优先复核建档承接字段：姓名、性别、年龄、外貌、性格、称号、天赋列表、出身背景、所属门派ID与关键初始资源，避免第1回合读取时出现空缺。',
    构建修炼体系附加块('- 若启用修炼体系，再优先复核建档承接的初始境界与其他修炼真值，避免第1回合读取时出现空缺。'),
    '- 优先确认第0回合已经坐实的时间终点、地点终点、主角当前状态、在场人物、正式新角色、NPC记忆、资源变化与任务约定落点。',
    '- 新增 `社交` 人物时，只补第1回合立刻要用到的最小完整档案，不把背景提及人物一口气全部写进前台。',
    '- 若正文已形成休整、赶路、开场战斗、物品获得、关系确认、正式约定或任务入口，就把这些结果直接落成前台变量。',
    '- 若某个字段只是建档信息、世界母设或远期规划，但第0回合正文还没让它变成当前前台事实，就不要抢先写入。',
    ...(params?.fandomEnabled
        ? [
            '- 同人模式下，变量落地也要贴住当前章节窗口：只把当前章已经前台成立的事实写进变量，不把下一组或下一章结果提前写入。'
        ]
        : [])
].join('\n');
