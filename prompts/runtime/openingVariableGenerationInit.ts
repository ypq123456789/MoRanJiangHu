import { 构建男娘NSFW附加块, 构建修炼体系附加块 } from '../../utils/promptFeatureToggles';

const 开局变量初始化覆盖清单 = [
    '【开局变量初始化覆盖清单】',
    '- 环境：时间、大地点、中地点、小地点、具体地点、天气、节日、环境变量。',
    '- 角色：基础档案、六维、金钱、精力/饱腹/口渴/负重、坐标、七部位血量与状态、装备、物品、技艺、BUFF。',
    构建修炼体系附加块('- 角色：若启用修炼体系，再覆盖境界/境界层级、经验、内力、功法、突破条件。'),
    '- 社交：本回合已经正式在前台成立、且第1回合立刻会用到的 NPC 最小完整档案、天赋列表、出身背景、技艺、七部位血量与状态、关系状态、记忆、是否在场等。',
    '- 战斗：是否战斗中、敌方列表、必要的轮次/阶段/战场效果。',
    '- 玩家门派：基础信息、玩家职位/贡献/累计贡献、兑换列表、重要成员；不要把门派任务放在玩家门派内部入口。',
    '- 任务列表：已正式存在的任务对象、目标列表、状态、奖励描述；类型必须归入主线/支线/门派/奇遇/悬赏/传闻。',
    '- 约定列表：已正式存在的约定对象、对象/地点/时间/状态/后果。',
    '- 世界.势力列表：优先承接世界观生成阶段已经落入当前世界基底的势力，不要另起一套新势力。若当前世界基底确实为空，才根据世界观补 5-15 个势力组织（门派/家族/商会/镖局/官府/帮派等），每个势力包含 ID、名称、类型、实力等级(1-10)、地盘归属、描述、代表性物品风格、关系网、库藏物品池(3-8件物品)、当前状态。势力类型分布合理并贴合题材；仙侠模式的库藏物品池要覆盖法宝、灵石、符箓、丹药、玉简、妖兽材料等。势力关系网标注友好/中立/敌对/从属/联盟。'
].join('\n');

export const 开局变量生成附加提示词 = [
    '【开局变量生成任务】',
    '- 当前任务是第0回合开局完成后的首轮前台变量生成，不是普通回合的回合后补录，也不是增量补丁。',
    '- 你要根据第0回合正文与开局变量规划，把第1回合立刻要用到的前台变量树一次落稳，让后续独立世界/规划链路直接建立在这份前台状态上。',
    '- 将第0回合 `<变量规划>` 视为自然语言的完整前台初始化说明稿，而不是增量摘要；优先按其中写明的当前终态、依据与对象结构生成最终变量命令。',
    '- 开局变量生成属于“首回合全域初始化审计”：必须逐域复核 `环境`、`角色`、`社交`、`战斗`、`玩家门派`、`任务列表`、`约定列表`，不能因为 `<变量规划>` 某域写得少就直接跳过该域。',
    '- 即使 `<变量规划>` 漏写部分字段，也要结合正文、建档承接结果、当前变量树与当前空壳状态，补齐第1回合最小完整可用结构。',
    '- 变量生成完成后，后续独立链路才会继续做世界初始化与规划初始化；因此你要优先把前台根路径的时间、地点、人物、资源、关系、任务与约定状态写准。',
    '- 本任务只处理正式变量根域：`环境`、`角色`、`社交`、`战斗`、`玩家门派`、`任务列表`、`约定列表`、`世界.势力列表`。其中 `世界.地图层级` 已由世界观生成阶段提供基底，不在本阶段重造。',
    '- 【强制】环境.大地点必须从世界观设定中读取世界名称（如世界观写"太古界"则大地点="太古界"），不要用区域/州府名（如"中州"）充当大地点。中地点才是区域/州府。',
    '- 只承认第0回合正文里已经前台成立的事实；背景设定、未来安排、章节后续与后台世界余波都不提前写成前台变量。',
    '- 主角建档里已经明确给出的 `姓名`、`性别`、`年龄`、`外貌`、`性格`、`称号`、`天赋列表`、`出身背景`、`所属门派ID` 等，只要第0回合把它们承接成当前出场身份，就必须完整落入 `角色` 根域，不能因为它们来自建档就漏写。',
    '- `角色.技艺` 是开局必备变量：武侠模式至少包含医术、毒术、机关、采集、鉴定、易容、潜行、经商等生活与江湖技艺，不主动写炼丹、炼器、符箓、阵法；仙侠模式才至少包含炼器、炼丹、医术、阵法、符箓、机关、采集、鉴定。默认可为未入门，但若建档、门派、天赋、出身、经历或第0回合正文已经说明某项技艺基础，必须写成对应初始等级、熟练度与描述。',
    '- 若本次开局配置为仙侠模式，开局变量必须给主角与所有初始 NPC 补齐 `灵根/灵根资质/当前灵力/最大灵力/当前神识/最大神识/丹田状态/道基状态/心魔值/功德/业力`；初始功法可使用 `法诀/术法/神通/遁法/阵诀`，法宝可作为 `类型=法宝` 物品落地。',
    '- `社交[i].天赋列表` 与 `社交[i].出身背景` 是 NPC 初始技艺的依据；新增 NPC 时必须同时补齐，并让 `社交[i].技艺` 的非零项能从天赋、出身、身份、职责或经历中解释；同时补齐 `头部/胸部/腹部/左手/右手/左腿/右腿` 的当前血量、最大血量和状态。',
    '- 若【本次开局配置】中提供了 AI 生成角色性别硬约束，变量生成新增 `社交[]`、`玩家门派.重要成员[]`、`战斗.敌方[]`、任务发布人或任务人物时，性别必须从允许列表中选择；合法明确性别包含 `男/女/男娘/扶她`，不得把 `男娘`、`扶她` 或带有稳定对应设定的角色改写成 `女`，不得补出未允许性别，也不得写未知/不详绕过。主角性别保持建档值。',
    '- `社交[i].当前装备` 与 `社交[i].背包` 只按正文、建档或设定中明确成立的事实初始化；未知槽位写“无”，没有明确随身物就写空数组。不要为了完整性给 NPC 或傀儡按身份硬塞衣物、内衣、袜鞋、武器、腰牌、干粮、疗伤散等默认物。',
    '- 若建档或开局变量规划显示 `所属门派ID=none`、`门派职位=无`、未加入门派、无门无派，`玩家门派` 必须落为空门派结构，且 `重要成员/任务列表/兑换列表/藏经阁列表` 全部为空；不得补同门、师兄弟、师父、门规或门派任务。',
    '- 若已加入门派且需要生成门派任务，必须写入全局 `任务列表[]` 且 `类型=门派`，不要写成 `玩家门派.任务列表[]` 独立任务入口。无限流团队任务写入全局 `任务列表[]` 时应使用 `支线/悬赏/传闻`，发布人为队长、资深者或团队协调人，奖励为团队贡献、奖励点登记、补给额度、团队能力库权限或具体补给，不要复述主神主线的存活倒计时。',
    '- 任务必须根据第0回合正文、主线承接点、当前地点、在场人物、组织近况和眼前冲突生成；不得复用固定任务名、固定目标或固定奖励模板。禁用模板名包括：问道初途、初入江湖、守住第一夜、站稳第一步、主神任务倒计时；也不得复用“完成一次入门试炼/确认水源药品哨位/完成一次现场确认”等万能目标句。',
    '- 无限流任务字段必须区分：`发布地点` 是主神空间、队伍房间、团队集合点或任务发布现场；`任务世界` 才是生化危机任务世界、任务世界<荒怨>、异形世界等世界级名称。不得把具体地点、房间、村庄、团队名称或队伍名写进 `任务世界`。',
    构建修炼体系附加块('- 若建档已明确初始境界等修炼真值，只要第0回合把它们承接成当前出场身份，也必须完整落入 `角色` 根域。'),
    构建修炼体系附加块('- `角色.功法列表` 必须由你根据第0回合正文、建档、门派、职位、贡献、境界、内力与出身因果生成；不要依赖或复述本地固定兜底名称。'),
    构建修炼体系附加块('- 若主角已加入门派且贡献/职位足以接触藏经阁，应生成符合该门派风格的可学功法并写入 `角色.功法列表`；若无门派但已有境界、内力、家传、散修或江湖经历，也应生成合理入门功法。只有明确凡人、未入境、不会武或不可能接触功法时才允许留空。'),
    '- 若开局正文已经让某些状态自然成立，就直接落成变量；若证据不足，就保持最小、保守、可承接的当前值。',
    '- 若第0回合已正式出场或建档的 NPC 命中“女性 + 主要角色 + 高颜值明确”，必须直接落成完整档案，不留 `未知 / 不详 / 待补充 / 普通 / 正常` 这类占位字段。',
    '- 若 NSFW 模式开启，上述重要女性还必须同步落成 `名器档案 / 失贞档案 / 首次亲密记录`：名器档案至少包含胸部/小穴/屁穴三条，每条给出名称、品质、稳定描述和机制效果；名器名称只写在变量层和内部机制字段，玩家可见正文不得直呼名称；若启用了名器世界书，AI 只选择名器，品质、修正、标签和说明必须从对应“固定机制效果表”复制；无名器也要写“无名器/无对应名器”的结构化条目。没有明确经历时，`首次亲密记录=[]` 且 `失贞档案.是否失贞=false`。',
    构建男娘NSFW附加块('- 若 NSFW 模式与“男娘 / 扶她相关 NSFW 内容”总开关同时开启，第0回合已正式出场或建档的 `男性/男娘/扶她 + 主要角色` NPC 也必须落成 `男娘设定 / 扶她设定 / 肉棒描述 / 屁穴描述 / 性癖 / 敏感点 / 首次亲密记录`。用户或设定指向男娘/扶她时，对应设定不得留空；未指向时写稳定否定说明。男娘不存在阴道交记录，但男娘首次肛交等同失贞，需要写入 `失贞档案`。'),
    '',
    开局变量初始化覆盖清单
].join('\n');

export const 构建开局变量生成承接提示 = (params: {
    currentGameTime?: string;
    openingRoleSetupText?: string;
    openingPartnerSetupText?: string;
    openingConfigText?: string;
}): string => [
    '【开局承接提示】',
    '- 当前是第0回合后的首轮变量生成；要把第1回合会读取的前台变量树逐域初始化到最小完整可用状态，不把它当普通补丁。',
    `- 当前游戏内时间参考：${(params.currentGameTime || '').trim() || '未知时间'}`,
    '- 这是开局正文完成后的第一次前台变量落地，后续世界初始化与规划初始化都会以这里产出的前台状态为基底。',
    '- 这里的 `<变量规划>` 是自然语言初始化说明稿；若它对某个根域写得不够细，不代表该根域可以不审计。',
    '',
    '【主角建档承接信息】',
    (params.openingRoleSetupText || '').trim() || '未提供显式主角建档信息。',
    '',
    '【开局同伴建档承接信息】',
    (params.openingPartnerSetupText || '').trim() || '未启用或未提供显式开局同伴建档信息。',
    '',
    '【本次开局配置】',
    (params.openingConfigText || '').trim() || '未提供额外开局配置。'
].join('\n');

export const 构建开局变量生成正文上下文 = (params: {
    openingBodyText?: string;
    openingVariablePlanText?: string;
    currentGameTime?: string;
    openingRoleSetupText?: string;
    openingPartnerSetupText?: string;
    openingConfigText?: string;
}): string => [
    '【第0回合开局变量生成上下文】',
    构建开局变量生成承接提示(params),
    '',
    '【第0回合完整正文】',
    (params.openingBodyText || '').trim() || '暂无',
    '',
    '【第0回合完整变量规划】',
    (params.openingVariablePlanText || '').trim() || '未提供显式变量规划，请完全依据正文推导。'
].join('\n');

export const 构建开局变量生成审计重点 = (params?: {
    fandomEnabled?: boolean;
}): string => [
    '【开局变量生成审计重点】',
    '- 先把 `<变量规划>` 读成“自然语言终态说明稿”：优先抓取当前终态、成立依据、新增对象或字段、本回合已发生变化，再回正文补足过程证据。',
    '- 然后按 7 大根域逐域审计：就算 `<变量规划>` 某域写得很短，也要判断该域是否需要首回合最小完整初始化。',
    '- 开局首回合优先保证：环境时间与地点可用、角色即时数值可用、在场 NPC 最小档案可用、门派/任务/约定引用可用，让第1回合能直接承接。',
    '- 角色根域必须优先复核建档承接字段：姓名、性别、年龄、外貌、性格、称号、天赋列表、出身背景、所属门派ID、技艺与关键初始资源，避免第1回合读取时出现空缺。',
    '- 若角色根域仍是无门派语义，门派根域也必须保持无门派语义，不得因为“门派状态需要可用”而虚构门派成员或同门关系。',
    构建修炼体系附加块('- 若启用修炼体系，再优先复核建档承接的初始境界与其他修炼真值，避免第1回合读取时出现空缺。'),
    构建修炼体系附加块('- 若启用修炼体系，再复核 `角色.功法列表`：功法应由 AI 按当前门派/身份/贡献/境界/内力/出身生成，不能用本地固定模板直接补齐。'),
    '- 优先确认第0回合已经坐实的时间终点、地点终点、主角当前状态、在场人物、正式新角色、NPC记忆、资源变化与任务约定落点。',
    '- 新增 `社交` 人物时，只补第1回合立刻要用到的最小完整档案；但一旦建档，就要同时补天赋列表、出身背景和由它们解释的初始技艺，不把背景提及人物一口气全部写进前台。',
    '- 若正文已形成休整、赶路、开场战斗、物品获得、关系确认、正式约定或任务入口，就把这些结果直接落成前台变量。',
    '- 若开局正文明确提到主角携带的货币金额（如"二两碎银"、"一百文铜钱"、"几枚金币"），必须同步写入 `角色.金钱`，不得因为背景配置未列货币而忽略正文已交代的初始资金。',
    '- 若某个字段只是建档信息、世界母设或远期规划，但第0回合正文还没让它变成当前前台事实，就不要抢先写入。',
    ...(params?.fandomEnabled
        ? [
            '- 同人模式下，变量落地也要贴住当前章节窗口：只把当前章已经前台成立的事实写进变量，不把下一组或下一章结果提前写入。'
        ]
        : [])
].join('\n');
