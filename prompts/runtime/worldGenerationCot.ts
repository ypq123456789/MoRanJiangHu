import { 构建修炼体系附加块 } from '../../utils/promptFeatureToggles';

export const 世界观生成COT提示词 = `
你必须先输出 \`<thinking>...</thinking>\`，再输出 \`<世界观>...</世界观>\`。
世界观正文以 2500 字内为软限制；若额外要求本身需要更大信息量，优先保证世界观完整、自洽、可长期使用。
思考区内容按以下步骤组织：

<世界观生成思考协议>
## 总体职责
- \`<thinking>\` 是展示给外部的思考过程；负责写成“本次世界观生成实际如何分析、确认了什么、哪些项未知”的具体过程，而不是只复述协议标题。
- 固定使用 \`Step0~Step7\` 连续输出；每个 \`Step\` 标题必须独占一行，标题下至少输出 3 条 \`-\` 列点。
- 每条 \`-\` 都必须写成结合当前输入后的结果句，例如：\`- 已给定题材为：……；当前额外要求为：……。\`
- 若信息缺失，明确写出“当前未知 / 用户未提供 / 暂不能确认”，让思考链保持连续、可复核。
- 输出编号保持在 \`Step0~Step7\`，每个 Step 各自成段，不扩展变体编号，也不合并多个 Step。
- \`<世界观生成思考协议>\` 只定义思考流程要求；真正展示给外部的推理过程必须写在 \`<thinking>\` 中。

Step0: 任务边界确认
- 确认本轮只生成“世界观母本”，不生成开场、不初始化变量、不替玩家定制。
- 在 \`<thinking>\` 中明确写出本轮允许产出与输出边界。
- 若用户附带了超出世界观母本的要求，必须明确标注哪些可吸收、哪些本轮不处理。

Step1: 输入回收与约束锚定
- 回收已给定的世界母本输入：题材、世界规模、王朝格局、宗门密度、额外要求。
- 在 \`<thinking>\` 中逐项写出当前已知输入、缺失输入、需要保守处理的输入。
- 明确哪些输入是硬约束，哪些只是风格偏好或可弹性吸收项。

Step2: 成长边界确认
- 先确定世界成长边界，明确本世界必须兼容武侠成长体系。
- 在 \`<thinking>\` 中写明本世界排除哪些仙侠化、玄幻化、常态超规格能力。
- 若用户要求里存在高规格元素，必须明确说明如何降级、转译或剔除。
${构建修炼体系附加块([
'- 先确定世界成长边界，明确本世界必须兼容“累计境界值”武侠成长体系。',
'- 境界部分只归纳“整体境界分配”和“不同年龄段的大致境界划分”，详细境界设定与逐层细则留在后续专门协议处理。'
].join('\n'))}

Step3: 核心闭环设计
- 设计“地理 - 势力 - 社会秩序 - 风险生态 - 资源稀缺度”的闭环结构。
- 在 \`<thinking>\` 中具体写出这些模块如何互相支撑，而不是只列名词。
- 若某一模块信息不足，必须明确写出当前采用的保守补位方式。

Step4: 世界逻辑校验
- 校验经济、治安、交通、信息传播、朝廷与宗门关系、黑市与灰色秩序、名望与地位流动是否自洽。
- 在 \`<thinking>\` 中指出潜在矛盾点，并写明准备如何修正或规避。
- 不只写“逻辑自洽”，而是补出至少几个实际检查点。

Step5: 强度与稀缺度校验
- 校验高手、秘籍、名器、传承、危险区域、宗门资源是否形成合理稀缺梯度。
- 在 \`<thinking>\` 中明确写出哪些资源必须稀缺、哪些可以相对常见、为什么。
- 若出现低风险高回报泛滥风险，必须明确记录并压回合理区间。
${构建修炼体系附加块([
'- 校验高境界强者是否形成合理稀缺梯度。',
'- 校验境界相关表述是否停留在“分配与年龄段划分”层级，让详细境界体系说明停留在本轮之外。'
].join('\n'))}

Step6: 玩家定制倾向剔除
- 排查玩家专属身份、玩家专属任务、主角天命叙事、围绕建档角色服务的世界结构。
- 在 \`<thinking>\` 中逐项说明哪些内容属于母本约束、哪些内容会导致世界观偏向玩家定制。
- 若输入中混入玩家导向信息，必须明确写出本轮如何剥离。

Step7: 最终产出校验
- 最终产出 world_prompt，要求可直接用于后续主 COT、开局初始化、世界演化与长期叙事约束。
- 在 \`<thinking>\` 中总结本轮世界观母本的核心约束、长期可用性与剩余未定项。
- 在确认思考闭合后，才允许输出 \`<世界观>...</世界观>\`。

输出门禁：
1. 必须先有 \`<thinking>\`，且必须闭合为 \`</thinking>\`。
2. \`</thinking>\` 之后只输出一个 \`<世界观>...</世界观>\` 标签块，其余解释文本留在输出之外。
3. 输出保持在 JSON 与 Markdown（如标题、列表、代码块）之外。
4. 输出内容保持在玩家专属或角色定制内容之外。
5. 世界观正文保持为母本结构，不写成开场剧情、任务提纲、人物小传或一次性场景描述。
6. 世界观必须形成“母本约束”，并能长期支撑后续系统，而不是只追求华丽设定堆砌。
7. 默认控制在 2500 字内；若额外要求明显需要更多篇幅，优先完整表达核心世界结构，再适度放宽字数。
</世界观生成思考协议>
`.trim();

export const 世界观生成COT伪装历史消息提示词 = `<think>
思考已结束
</think>
好的，将先以<thinking></thinking>输出思考，再以<世界观></世界观>输出世界观正文，且不使用Markdown，不生成玩家定制内容，不做变量初始化：`;
