export const 剧情规划变量结构提示词 = [
    '<剧情规划变量结构>',
    '# 【剧情 / 规划变量结构协议】',
    '',
    '## 0. 边界与模式',
    '- 章节状态固定写在 `剧情.*`。',
    '- 原创模式只写 `剧情规划.*` 与 `女主剧情规划.*`。',
    '- 同人模式只写 `同人剧情规划.*` 与 `同人女主剧情规划.*`。',
    '- 原创规划树与同人规划树互斥使用；不要跨树补字段，也不要把一套结构的字段借到另一套根路径里。',
    '- 运行时保留数组原形；若数组元素是对象，阅读视图中通常会额外带一行 `"[0]"` / `"[1]"` 这类锚点用于阅读定位，真实结构仍是数组。',
    '- 任何标注为 `string[]` / `number[]` 的字段，即使只有 1 条内容，也必须保持数组写法；不要把数组字段降成单个字符串或数字。',
    '- 命令保持最小路径更新，不使用 `set 剧情 = {...}`、`set 剧情规划 = {...}` 这类整对象覆盖。',
    '- `已完成摘要[] / 已成立事实[] / 沉淀内容[] / 归档内容[]` 这类累计数组，新增单条时优先用 `push`；只有修正旧条目或整组重建时才用 `set`。',
    '- `世界.进行中事件[i].当前进展` 是单值字段；规划或世界链若要修它，应直接 `set` 成新的当前摘要，不做数组追加。',
    '',
    '## 1. 章节状态 `剧情`',
    '- `剧情.当前章节: { 标题, 当前分解组, 原著章节标题, 原著推进状态, 原著换章条件[], 原著切换说明[], 已完成摘要[], 当前待解问题[], 切章后沉淀要点[] }`',
    '- `剧情.下一章预告: { 标题, 大纲[], 进入条件[], 风险提示[] }`',
    '- `剧情.历史卷宗[i]: { 标题, 所属章节范围, 所属分解组, 章节总结[], 延续事项[], 关系变化[], 势力变化[], 地点变化[], 资源变化[], 分歧线变化[], 记录时间 }`',
    '- `剧情` 只承载章节核心状态、预告和历史沉淀，不再混入规划池。',
    '',
    '## 2. 原创剧情规划 `剧情规划`',
    '- `剧情规划.当前章目标: string[]`',
    '- `剧情规划.当前章任务[i]: { 标题, 任务说明, 计划执行时间, 最早执行时间, 最晚执行时间, 前置条件[], 触发条件[], 阻断条件[], 执行动作[], 完成判定[], 失败后转移[], 完成后沉淀[], 关联人物[], 关联地点[], 关联势力[], 当前状态 }`',
    '- `剧情规划.跨章延续事项[i]: { 标题, 延续原因[], 当前状态[], 延续到何时, 后续接续条件[], 终止条件[] }`',
    '- `剧情规划.待触发事件[i]: { 事件名, 事件说明, 计划触发时间, 最早触发时间, 最晚触发时间, 前置条件[], 触发条件[], 阻断条件[], 成功结果[], 失败结果[], 当前状态 }`',
    '- `剧情规划.镜头规划[i]: { 镜头标题, 镜头内容, 触发时间, 前置条件[], 触发条件[], 阻断条件[], 关联任务[], 关联人物[], 关联地点[], 沉淀内容[], 当前状态 }`',
    '- `剧情规划.换章规则: { 本章完成判定[], 允许切章条件[], 禁止切章条件[], 切章后需沉淀内容[], 切章后需清空字段[], 切章后需重建字段[] }`',
    '',
    '## 3. 原创女主剧情规划 `女主剧情规划`',
    '- `女主剧情规划.阶段推进[i]: { 阶段名, 阶段目标[], 主推女主[], 次推女主[], 禁止越级对象[], 关联剧情任务[], 阶段完成判定[], 切换条件[] }`',
    '- `女主剧情规划.女主条目[i]: { 女主姓名, 类型, 当前关系状态, 当前阶段, 已成立事实[], 阶段目标[], 推进方式[], 阻断因素[], 允许突破条件[], 失败后回退[] }`',
    '- `女主剧情规划.女主互动事件[i]: { 女主姓名, 事件名, 事件说明, 计划触发时间, 最早触发时间, 最晚触发时间, 前置条件[], 触发条件[], 阻断条件[], 成功结果[], 失败结果[], 关联剧情任务[], 当前状态 }`',
    '- `女主剧情规划.女主镜头规划[i]: { 女主姓名, 镜头标题, 镜头内容, 触发时间, 触发条件[], 关联事件[], 关联剧情任务[], 沉淀内容[], 当前状态 }`',
    '- 女主推进按“阶段”组织：阶段里直接写主推女主、次推女主、禁止越级对象、完成判定与切换条件。',
    '',
    '## 4. 同人剧情规划 `同人剧情规划`',
    '- `同人剧情规划.当前对齐信息: { 当前分解组, 当前章节范围, 当前章节标题[], 当前承接方式, 当前原著状态[], 当前已形成偏转[] }`',
    '- `同人剧情规划.当前章目标: string[]`',
    '- `同人剧情规划.当前章任务[i]: { 标题, 任务说明, 关联分解组:number[], 关联原著事件:string[], 保持不变的原著基线:string[], 当前偏转点:string[], 计划执行时间, 最早执行时间, 最晚执行时间, 前置条件[], 触发条件[], 阻断条件[], 执行动作[], 完成判定[], 偏转后果[], 未偏转后果[], 完成后沉淀[], 当前状态 }`',
    '- `同人剧情规划.分歧线[i]: { 分歧线名, 起点事件, 关联分解组:number[], 偏转原因[], 与原著不同之处[], 当前阶段, 影响范围[], 下一步扩大条件[], 回收条件[], 当前状态 }`',
    '- `同人剧情规划.待触发事件[i]: { 事件名, 事件说明, 关联分解组:number[], 关联原著事件:string[], 计划触发时间, 最早触发时间, 最晚触发时间, 前置条件[], 触发条件[], 阻断条件[], 触发后影响[], 错过后影响[], 若偏转则转入哪条分歧线:string[], 当前状态 }`',
    '- `同人剧情规划.镜头规划[i]: { 镜头标题, 关联分解组:number[], 镜头内容, 触发时间, 触发条件[], 关联人物[], 关联地点[], 关联分歧线[], 作用[], 当前状态 }`',
    '- `同人剧情规划.换组规则: { 当前组完成判定[], 下一组进入条件[], 禁止换组条件[], 换组后沉淀内容[], 换组后需清空字段[], 换组后需重建字段[] }`',
    '',
    '## 5. 同人女主剧情规划 `同人女主剧情规划`',
    '- `同人女主剧情规划.阶段推进[i]: { 阶段名, 关联分解组:number[], 主推女主[], 次推女主[], 关联分歧线[], 阶段目标[], 禁止越级对象[], 完成判定[], 切换条件[] }`',
    '- `同人女主剧情规划.女主条目[i]: { 女主姓名, 类型, 关联分解组:number[], 关联原著关系线:string[], 保持不变的原著基线:string[], 当前偏转点:string[], 所属分歧线:string[], 当前关系状态, 当前阶段, 已成立事实[], 阶段目标[], 推进方式[], 阻断因素[], 允许突破条件[], 失败后回退[] }`',
    '- `同人女主剧情规划.女主互动事件[i]: { 女主姓名, 事件名, 事件说明, 关联分解组:number[], 关联原著事件:string[], 关联分歧线:string[], 计划触发时间, 最早触发时间, 最晚触发时间, 前置条件[], 触发条件[], 阻断条件[], 成功结果[], 失败结果[], 与主剧情联动[], 当前状态 }`',
    '- `同人女主剧情规划.女主镜头规划[i]: { 女主姓名, 关联分解组:number[], 镜头标题, 镜头内容, 触发时间, 触发条件[], 关联事件[], 关联分歧线[], 沉淀内容[], 当前状态 }`',
    '',
    '## 6. 维护纪律',
    '- 规划池只保留未来项；本回合已发生、已完成、已失效、已错过、已取消的内容必须迁移、改状态或清理。',
    '- `镜头规划` 必须保留；它负责承接场外动态、群像呼应与氛围推进，不得删除。',
    '- 所有任务、事件、镜头都要尽量补齐时间、前置条件、触发条件、阻断条件和状态。',
    '- 切章/换组前，当前章任务、待触发事件，以及启用时与当前章直接相关的女主互动事件，必须优先全部进入终态：已完成、已结算、已失效、已过期、已取消，或已明确迁移为跨章承接结构。',
    '- 只要还有属于当前章的任务或事件停留在未触发、待触发、可触发、推进中、待结算、待迁移这类未终态，就不得切章/换组。',
    '- `剧情.历史卷宗` 只能在真正切章/换组时 `push`；平时不记录单回合流水。',
    '- 切章/换组顺序固定为：先把旧章 `已完成摘要` 与切章沉淀结果压入 `剧情.历史卷宗`，再清空旧章专属内容与当前章规划池，最后结合旧章已完成摘要和 `【下一章节内容】` 完整初始化新章。',
    '- 若额外注入 `【前一章节内容】 / 【当前章节内容】 / 【下一章节内容】`，当前章是执行基准，前后章只用于承接与防抢跑。',
    '',
    '</剧情规划变量结构>'
].join('\n');
