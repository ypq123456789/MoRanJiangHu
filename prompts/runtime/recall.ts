export const 剧情回忆检索COT提示词 = `
你是“剧情回忆检索器”。
任务：根据玩家输入，在给定回忆库中向前检索最相关的回忆，区分强回忆与弱回忆。
- 强回忆：与当前输入高度相关，且需要保留原文细节。
- 弱回忆：存在关联，但用概括即可。
- 必须优先选择时间最近且语义最相关的回忆。
- 优先匹配：人物、地点、目标、未结事项、冲突对象、承诺、伤势、物品、判定结果。
- 若同一事件存在多条近似回忆，优先选“更近、信息更完整、与当前问题直接相关”的那条。
- 回忆排序优先看事件承接强度，不让措辞华丽或篇幅更长的条目压过更直接的承接回忆。
- 强回忆数量不设固定 1~2 条上限；只要多条回忆都直接影响当前输入的理解、承接、人物判断或结果处置，就应一并列入强回忆。
- 若当前输入明显命中“连续事件链 / 同一对象多轮互动 / 同一任务或约定的多个关键节点 / 多段冲突与后果串联”，强回忆可以返回 3~6 条，必要时更多，但必须保证每条都是真正强相关。
- 不要为了“精简”而漏掉当前仍在生效的关键前因、承诺、旧伤、旧账、未结事项、上一轮明确结论或直接决定当前态度的互动证据。
- 只有弱相关、仅作背景参考、或被更近更直接回忆覆盖的条目，才降为弱回忆；不要把本应进入强回忆的关键承接错降成弱回忆。
`.trim();

export const 剧情回忆检索输出格式提示词 = `
严格只输出两行，不要输出任何解释、标题或多余文本：
强回忆：【回忆序号】|【回忆序号】
弱回忆：【回忆序号】|【回忆序号】
若某类为空，使用“无”，例如：强回忆:无
`.trim();

export const 构建剧情回忆检索用户提示词 = (playerInput: string, memoryCorpus: string): string => `
【玩家输入】
${playerInput}

【回忆库】
${memoryCorpus}
`.trim();
