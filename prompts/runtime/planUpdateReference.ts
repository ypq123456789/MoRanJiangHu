import { 剧情规划变量结构提示词 } from './storyPlanSchema';

const 规划补丁输出约束 = [
    '<规划补丁输出协议>',
    '# 【规划补丁输出协议】',
    '- 思考统一按本任务的 Step0~Step9 完成完整审计。',
    '- 本任务以审计现有规划池并做最小补充/调整为主，不重做整套规划。',
    '- Step0~Step2 先形成事实快照与是否需要更新的初步结论；若无需更新，后续 Step 仍要围绕维持原状的依据完成审计。',
    '- 只允许输出 `<thinking>`、`<说明>`、`<命令>` 三个标签块。',
    '- `命令` 只允许 `set|add|push|delete`，且每条命令只改一个最小路径；规划分析默认以 `set / push / delete` 为主，只有命中明确数值字段时才使用 `add`。',
    '- 命令顺序固定为“先 `set/add`，再 `push`，最后 `delete`”；若同一数组存在多个 `delete`，按索引从大到小输出。',
    '- 命令保持最小路径更新，不使用 `set 剧情 = {...}`、`set 女主剧情规划 = {...}` 这类整对象覆盖。',
    '- `push` 只用于补齐现有结构未覆盖、且已被本回合事实明确锚定的未来承接点，也可用于在事实充足时恢复被清理后不足的有效数量。',
    '- 若证据不足、未命中触发条件或无法形成确定补丁，`<说明>` 输出 `- 无需更新`，`<命令>` 输出 `无`。',
    '- `<说明>` 只记录清理/修订依据，不写解释性散文。',
    '- `<命令>` 每行必须使用 `set|add|push|delete 路径 = 值`；规划分析默认以 `set / push / delete` 为主，只有命中明确数值字段时才使用 `add`。',
    '</规划补丁输出协议>'
].join('\n');

const 统一规划分析独立COT = [
    '<统一规划分析思考协议>',
    '# 【统一规划分析思考协议｜独立规划版】',
    '',
    '## 0. 使用边界',
    '- 本协议只服务“每回合规划分析”，不生成正文，不处理判定，不处理战斗，不代写玩家行为。',
    '- 思考统一写在 `<thinking>` 中，固定使用 `Step0~Step9`；每个 Step 标题独占一行，标题下至少输出 3 条 `-` 列点。',
    '- `<thinking>` 只负责记录本回合已命中的事实、结构审计、取舍过程、风险点与命令前置判断；完整补丁命令留到 `<命令>` 输出。',
    '- 可写路径固定落在 `剧情.*` 与当前激活规划树；只有当前上下文同时提供 `<女主剧情规划协议>` 与 `<女主剧情规划思考协议>` 时，才额外审计并写入女主规划树。',
    '- 本协议只处理规划层的清理、修订、补位、迁移与承接校对；角色数值、普通状态字段与正文结果保持在各自模块处理。',
    '- 规划分析不读取本回合主剧情 `<命令>` 作为待审草稿；是否补承接、修状态或切章，只依据“当前状态 + 最近完整正文 + 本回合 `<剧情规划>`”。',
    '- 若小说分解滑窗条目标出 `谁知道 / 谁不知道 / 是否仅读者视角可见`，必须按它约束规划判断：读者暗线只能作为风险和伏笔，不直接升级成角色已知前提。',
    '',
    '## 1. `<thinking>` 输出格式',
    '- 必须按 `Step0` 到 `Step9` 顺序连续输出，不合并 Step，不扩展变体编号。',
    '- 每个 Step 优先写：已知事实、当前结构状态、发现的问题、候选取舍、降级结论、对补丁的影响。',
    '- 信息缺失时直接写明“当前未知 / 上下文未提供 / 暂不能确认”，让审计链条保持连续、可复核。',
    '',
    'Step0: 门禁、上下文校对与任务边界',
    '- 先确认当前输入是否包含：最近完整正文、当前剧情状态、当前环境、当前世界状态、本回合规划与思考；若上下文同时提供 `<女主剧情规划协议>` 与 `<女主剧情规划思考协议>`，再一并检查女主规划与社交状态。',
    '- Step0 的第一审计动作是“当前分解组对位”：先读取 `剧情.当前章节.当前分解组`，再对照滑窗里的 `【当前章节内容】 / 【下一章节内容】` 的 `【组号】 / 【章节范围】 / 【章节标题】 / 【本组概括】` 与最近完整正文，判断当前存档是否还停在正确分解组。',
    '- 明确本回合写入边界：只修订规划，不补正文，不把旧规划反推成本回合新事实。',
    '- 若最近完整正文、当前状态与本回合 `<剧情规划>` 已经主要落在 `【下一章节内容】` 的开局已成立事实、前组延续事实、主场景或主矛盾上，就把它标记为“当前分解组疑似未切换”，后续各 Step 必须优先审计换组/换章，而不是继续按旧组补位。',
    '- 若关键上下文缺失、结构严重失真或证据不足，先下“仅允许清理 / 修订 / 不更新”的门禁结论。',
    '- 切章所有权只属于本链路；若本回合没有真正切章，就不要改写章节定位，也不要写 `剧情.历史卷宗`。',
    '- 切章属于高门槛操作：先过“任务/事件清空门禁”，再过“当前章收束 + 当前章规划池收尾 + 当前章世界余波可承接 + 下一章起点已成立”四道门禁；任一未过，本回合默认不切章。',
    '',
    'Step1: 本回合事实提取与规划快照',
    '- 从最近完整正文提取本回合已发生的明确事实，区分为：已完成、已受阻、已失效、形成新承接、仍未结算五类。',
    '- 先参考【本回合<剧情规划>】中的 `<剧情规划>` 摘要理解主剧情主动保留了哪些承接、延后了哪些推进；这些内容只作辅助理解，不覆盖正文事实。',
    '- 建立本回合时间、地点、主要参与者、关键后果、主线承接点，以及当前规划池的整体快照。',
    '- 提取事实时同步标记它们更像属于“当前组收尾 / 下一组开场 / 跨组延续”哪一类，避免把已经进入下一组的事实继续挂在旧组下修补。',
    '- 若滑窗已提供 `关键事件 / 角色推进 / 原著硬约束 / 可提前铺垫`，先提炼当前组的情感基线与信息差：谁已经知情、谁仍误会、谁在愧疚/担心/防备；这些中间层级只在对应层级修订，不把误会型冲突直接改成死仇型规划。',
    '- 把正文中只是气氛、猜测、试探、未兑现承诺或未落地意图的内容，保持在观察层，不直接当成规划事实。',
    '',
    'Step2: 当前结构读取与初步结论',
    '- 读取 `剧情.*` 当前结构，确认当前章节、下一章预告、历史卷宗，以及当前激活主线规划树的现状。',
    '- 若上下文同时提供 `<女主剧情规划协议>` 与 `<女主剧情规划思考协议>`，再读取当前激活女主规划树的阶段推进、女主条目、女主互动事件与女主镜头规划现状。',
    '- 再读取当前世界树，确认与当前章直接相关的 NPC 行动、待执行事件、进行中事件、世界镜头和地点余波是否已经完成、失效、迁移或跨章化。',
    '- 在此 Step 明确给出“当前分解组仍对齐 / 当前分解组疑似落后需切到下一组或下一章 / 证据不足暂不切换”的初步判断；只要已出现错位信号，后续所有规划修订都要先服从这个判断。',
    '- 先给出“无需更新 / 需要局部修订 / 需要清理 + 修订 / 需要补位 / 绝不允许切章”的初步判断，让后续审计围绕结论展开。',
    '',
    'Step3: 主线结构审计',
    '- 检查 `剧情.*` 中哪些条目已经被正文结算、被正文否定、时间已过、条件失效，或仍然有效待承接。',
    '- 核对当前章节、下一章预告、历史卷宗，与当前激活主线规划树的当前章任务/跨章延续事项/待触发事件/镜头规划/换章或换组规则之间是否存在断档、重复、冲突或时序倒挂。',
    '- 逐项检查当前章任务、待触发事件、镜头规划和当前待解问题里，哪些仍属于当前章未收束项；其中任务与事件必须优先进入终态，未完成、未失效、未过期、未取消、未迁移的项会天然阻止切章。',
    '- 若正文事实已经明显进入下一组或下一章，就禁止继续往旧 `当前分解组` 下追加新任务、新镜头或新待解问题；这类修订必须先判断是否应切组、迁移或归档。',
    '- 若正文形成了新的主线落点，而当前剧情结构尚未覆盖，记录为“可补最小锚点”的候选，不直接整段重写。',
    '',
    'Step4: 女主结构与跨树联动审计',
    '- 若上下文同时提供 `<女主剧情规划协议>` 与 `<女主剧情规划思考协议>`，检查阶段推进、女主条目、女主互动事件、女主镜头规划是否仍与正文一致。',
    '- 核对关系推进是否只落在正文已支持的层级；感谢、欠情、试探、合作、受阻、约定等结果停留在对应承接层，不拔高为突破已完成。',
    '- 再检查主线推进后女主线是否断档、以及女主推进后主线是否缺少变量、事件、章节或镜头承接；若未注入上述两个协议，则明确收口为“本回合不新增女主规划树”。',
    '- 女主互动事件与女主镜头规划中，只要仍有属于当前章且未完成、未迁移、未失效的项，也要阻止切章；其中女主互动事件同样必须优先进入终态。',
    '- 同人模式下，原著角色与女主的关系推进必须回扣当前章原著情感方向；若当前原著进度只支持误会、自卫、亏欠、试探、担忧或暂时疏离，就不得把主线或女主线修成厌恨、彻底决裂或不死不休。',
    '- 同步审计女主规划数量是否被旧项挤占：`女主条目` 常态 1~4、峰值 6；`女主互动事件` 常态 3~6、峰值 8；`女主镜头规划` 常态 2~5、峰值 6。超额时本回合先清旧项，不先补位。',
    '',
    'Step5: 清理、修订与补位候选',
    '- 汇总所有应处理对象：已发生仍留在未来池的项、已失效项、已取消项、被正文否定项、悬空引用项、重复主键项，以及需要修订的状态、触发条件、失效时间、承接点与引用关系。',
    '- 把候选变化拆成“删除 / 改状态 / 迁移 / 修订 / 新增最小锚点 / 延后观察”几类，避免把仍有承接价值的对象一刀切删除。',
    '- 只有当正文已明确形成新承接、现有结构缺口明显、且可用当前字段树直接锚定时，才保留新增候选；其余情况优先清理与修订。',
    '- 若本回合要切章，必须先把当前章未完成但允许带走的内容迁成跨章延续事项、世界待执行事件、世界镜头规划或其他稳定承接结构，再进入切章候选。',
    '- 进入切章候选后，先预演旧章如何归档进 `剧情.历史卷宗`，再预演哪些旧章字段和当前章规划池要清空，最后再预演新章如何依据旧章已完成摘要与 `【下一章节内容】` 完整初始化。',
    '- 对女主规划，默认顺序固定为“先清已结算旧项，再修状态，再补最小新承接”；不要为了补数量把已失效旧项继续挂池。',
    '',
    'Step6: 最终取舍与无更新判断',
    '- 把本回合变化拆成“清理 / 修订 / 新增 / 迁移 / 切章 / 无需更新”六类，并说明每类的成立依据。',
    '- 先判断哪些变化是必须落命令的确定项，哪些只是观察结论，不进入补丁层。',
    '- 若存在多种修法，优先选择最小改动、承接最稳、不会制造新断档的一种；若全部候选都只停留在弱信号层，就回收为“无需更新”。',
    '- 若任务/事件清空门禁或四道切章门禁没有全部通过，就明确收口为“本回合不切章”，并把精力放在补承接、修状态、推进结算和迁移未收束项上。',
    '',
    'Step7: 命令前置排序与路径确认',
    '- 按“结算迁移 -> 推进补位 -> 修订校正 -> 清理收尾 -> 切章重建”排序预演命令，确认每条命令只触及一个最小路径。',
    '- 校对路径是否真实存在、数组索引是否可追、写入后是否会造成整对象覆盖或跨树污染。',
    '- 命令只使用 `set|add|push|delete`；规划分析默认以 `set / push / delete` 为主，只有命中明确数值字段时才使用 `add`；当前事实不支持时，不预演对应命令。',
    '- 若已判断当前分解组错位，优先预演修正 `剧情.当前章节.当前分解组` 与相关章节定位字段的命令，再决定旧组内容如何归档、迁移、清空或延续；不要先按旧组补规划，最后才想起切组。',
    '- 若进入切章候选，必须预演新的 `当前分解组 / 原著章节标题 / 原著推进状态 / 原著换章条件 / 原著切换说明 / 标题` 等定位字段，而不是只预演一个 `原著推进状态 = 已完成`；新的 `当前分解组` 必须切到 `【下一章节内容】` 的分解组号。',
    '',
    'Step8: 证据强度与一致性复核',
    '- 逐项检查每个候选变化是否有足够正文证据、时间依据、结构依据、世界依据与承接价值。',
    '- 复核更新后是否仍保持：只含未来项、引用闭合、时序正确、主线与女主线承接同步、世界余波可追踪、无重复无冲突。',
    '- 重点复核更新后是否还把下一组或下一章已经成立的事实继续挂在旧 `当前分解组` 下；若仍存在这种错位，必须回退并改成先修章节定位。',
    '- 若发现写入会制造新断档、新冲突、未收束切章或越权字段，回退到更小改动或取消更新。',
    '',
    'Step9: 最终收口',
    '- 用结果句总结本回合最终保留哪些确定变化、哪些被清理、哪些被修订、哪些被迁移、哪些延后观察、是否允许切章。',
    '- 若结论是无需更新，明确收口为“现有结构继续延续”，并给出简明依据。',
    '- 让 `<说明>` 与 `<命令>` 只承接本 Step 已确认的结果，不额外扩写新判断。',
    '',
    '</统一规划分析思考协议>'
].join('\n');

export const 构建统一规划分析专用上下文 = (): string => [
    '【规划结构参考（统一规划分析专用）】',
    剧情规划变量结构提示词,
    '- 只有当前上下文同时提供 `<女主剧情规划协议>` 与 `<女主剧情规划思考协议>` 时，才允许额外写入 `女主剧情规划.*`。',
    '- 命令保持最小路径更新；数组路径必须精确，例如 `剧情规划.待触发事件[0].最晚触发时间`、`女主剧情规划.女主互动事件[1].当前状态`。',
    '- 规划分析只依据“当前状态 + 最近完整正文 + 本回合 `<剧情规划>`”判断是否要补承接，不把本回合主剧情 `<命令>` 当作输入草稿。',
    '- `剧情.当前章节.当前分解组` 是小说分解滑窗的硬对齐字段；必须与 `【当前章节内容】 / 【下一章节内容】` 的组号、章节标题、章节范围和最近正文交叉校对，发现正文已落到下一组时优先修正。',
    '- 当前激活女主规划树的重点结构：阶段推进 / 女主条目 / 女主互动事件 / 女主镜头规划。',
    '- 两侧规划都只保留未来项；本回合已发生、已失效、已取消、已被正文否定的项，必须清理、迁移或改状态。',
    '- 女主规划要维护数量：`女主条目` 常态 1~4、峰值 6；`女主互动事件` 常态 3~6、峰值 8；`女主镜头规划` 常态 2~5、峰值 6。超额时先清旧项，不足时只在有明确承接时补位。',
    '- 女主线推进后，若已形成主剧情承接点，必须同步检查 `剧情.*` 是否存在可追踪落点；若主线已推进，也要检查女主线是否出现断档或失真。',
    '- 最近完整正文是唯一新增事实来源；旧规划只能作为待修对象，不能反推成本回合新事实。'
].join('\n');

export const 统一规划分析COT提示词 = [
    统一规划分析独立COT,
    [
        '<统一规划分析专项协议>',
        '# 【统一规划分析专项协议】',
        '- 本任务是“每回合规划分析”，不是主剧情正文生成，也不是为了维护池子而维护池子。',
        '- 你要先基于本回合已发生正文与当前状态，判断 `剧情.*` 和 `女主剧情规划.*` 哪些地方需要同步、修订、清理或补承接。',
        '- 本回合 `<剧情规划>` 会作为辅助材料提供，帮助你理解主剧情本回合如何取舍、哪些推进被主动延后；若与正文冲突，正文优先。',
        '- 进入 Step0 前，先做一次“当前分解组错位审计”：不要把现有 `剧情.当前章节.当前分解组` 当成天然正确值，必须拿它去对照 `【当前章节内容】 / 【下一章节内容】` 与最近完整正文。',
        '- 若最近完整正文、当前状态与本回合 `<剧情规划>` 已明显进入 `【下一章节内容】` 的开局事实、前组延续事实、主场景或主矛盾，就必须优先判断是否应立即更新 `剧情.当前章节.当前分解组` 及相关章节定位字段，而不是继续按旧组补规划。',
        '- 若两侧规划都已经自洽且能承接后续，可以明确输出“无需更新”。',
        '- 先审计主线，再审计女主线，再检查两侧承接是否断裂、重复、冲突或时序倒挂。',
        '- 新增条目只用于补齐“本回合已明确出现但变量尚无承接”的最小锚点，让未来节点建立在现有证据上。',
        '- Step0~Step2 先完成事实与结构快照；Step3~Step5 审计主线、女主线与候选变化；Step6~Step9 完成取舍、命令预演与最终复核。',
        '- 输出可以是单一命令流，但命令路径必须天然区分 `剧情.*` 与 `女主剧情规划.*`。',
        '- 只有当前上下文同时提供 `<女主剧情规划协议>` 与 `<女主剧情规划思考协议>` 时，才允许处理 `女主剧情规划.*`；否则只处理 `剧情.*`。',
        '</统一规划分析专项协议>'
    ].join('\n'),
    规划补丁输出约束
].join('\n\n');
