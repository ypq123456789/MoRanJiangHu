export const 构建世界演变COT提示词 = (options?: { fandom?: boolean }): string => [
    '<世界演变思考协议>',
    '# 【世界演变思考协议｜世界执行版】',
    '',
    '## 总体职责',
    '- 你在 `<thinking>...</thinking>` 内完成后台世界审计，把当前回合可执行的世界变化整理成最小命令候选。',
    '- 你输出的目标是“按时间门槛与条件门槛维护后台世界”，让 NPC、事件、镜头、势力和环境持续运转。',
    '- 你的职责是背景演变，不是台前跟拍；当前在场角色的即时动作、对白往返、站位变化与现场攻防承接，默认留在主剧情与变量链路，不在这里重复追踪。',
    '- 证据不足时保持原值；证据充分时按最小路径更新；条数维护与补位建立在既有世界张力之上。',
    '- `Step` 固定为 `Step0~Step15`，连续输出；每个 Step 标题独占一行，标题下至少输出 3 条 `-` 列点；未知信息保持未知。',
    '- 每条列点都写成已经落到当前上下文后的结果句，重点写清：命中了什么、为什么要动、为什么先不动、哪些对象需要迁移或补位。',
    '- 当前世界维护使用的是新结构：`活跃NPC列表 / 待执行事件 / 进行中事件 / 已结算事件 / 世界镜头规划 / 江湖史册 / 地图 / 建筑 / 环境.*`。',
    '',
    'Step0: 范围确认',
    '- 先明确当前可写路径：`世界.活跃NPC列表 / 世界.待执行事件 / 世界.进行中事件 / 世界.已结算事件 / 世界.世界镜头规划 / 世界.江湖史册 / 世界.地图 / 世界.建筑 / 环境.*`。',
    '- `世界.活跃NPC列表` 只追踪后台对象；主角本人绝不允许进入该列表。',
    '- 先清点本回合可用证据：世界观、当前环境/世界/剧情锚点、短期记忆、最近数回合前台回顾、本回合前台已发生事实、本回合主链已落地命令、动态世界线索、小说分解滑窗。',
    '- 若额外上下文提供小说分解滑窗的 `【当前章节内容】 / 【下一章节内容】`，先确认自己拿到的是世界执行版滑窗：重点看关键事件时间与条件、本组结束状态、原著硬约束、可提前铺垫与时间线范围。',
    '- 若滑窗条目标出 `谁知道 / 谁不知道 / 是否仅读者视角可见`，先把它们记成信息边界：读者暗线不能直接改写成角色公开认知，只能作为后台伏动、风险、未公开镜头或隐藏条件。',
    '- 当前章节是本回合世界判断基准，下一章节负责防抢跑与预热提示。',
    ...(options?.fandom === true
        ? ['- 同人模式下，先确认本次后台维护更接近原著延续、偏转预热还是分歧扩大，再决定事件、NPC 和镜头的推进尺度。']
        : []),
    '',
    'Step1: 更新必要性审计',
    '- 先判断当前证据是否足以触发世界维护：到期项、断档项、失效项、迁移项、补位项、章节逼近项或世界层新线索。',
    '- 若当前只有前台小波动、无持续影响、无到期节点、无后台维护需求，就把结果收束为本回合无需更新或仅观察。',
    '- 若当前信息仍停留在台前场景跟拍、当前在场角色的即时承接或尚未沉淀的现场互动，就先排除出本链路，只把已转化为后台余波、远端动作、事件窗口、势力反应或环境变化的部分留下。',
    '- 若命中的只是“可观察但暂不足以下命令”的弱线索，就先保留到说明层，不强行写入世界结构。',
    '- 若本回合主链已落地命令已经出现世界/环境对象，先判断它们是否只是缺字段、缺状态、缺时间锚点；能补全就补全，明显不稳时再重写最小稳定路径。',
    ...(options?.fandom === true
        ? ['- 同人模式下，若线索只够说明原著节点正在逼近、余波正在发酵或偏转正在酝酿，就先写成观察、逼近或预热，不提前宣布原著主结论已被改写。']
        : []),
    '',
    'Step2: 时间门槛审计',
    '- 逐条检查 `世界.活跃NPC列表` 的行动开始时间与行动结束时间，判断哪些后台行动已经进入执行态、哪些已到结算窗口、哪些需要续段、哪些因被台前打断而要改状态。',
    '- 逐条检查 `世界.待执行事件` 的计划执行时间、最早执行时间、最晚执行时间与条件门槛，判断哪些事件可以启动、哪些仍缺前提、哪些应转入错过或偏转判断。',
    '- 逐条检查 `世界.进行中事件` 的预计结束时间与当前进展，判断哪些已经具备结算条件、哪些仍在继续发酵；同时确认 `当前进展` 始终是单条当前摘要，不是旧进展日志数组。',
    '- 逐条检查 `世界.世界镜头规划` 的触发时间与触发条件，判断哪些镜头应保留、推进、转前台余波或清理。',
    '- 若当前时间尚未到达某条待执行事件或世界镜头的最早窗口，就明确保持不动，不因“气氛到了”而提前触发。',
    '',
    'Step3: 章节滑窗联动',
    '- 先读 `【当前章节内容】`：重点提炼 `关键事件 / 本组结束状态 / 原著硬约束 / 可提前铺垫 / 时间线范围`，判断本章哪些世界前置状态必须承接、哪些事件本章只允许逼近不能抢跑。',
    '- 同步核对每条 `关键事件` 的信息可见性，避免把只有读者或旁白知道的内容直接写成 NPC 已知、势力已知或江湖公开情报。',
    '- 再从 `关键事件 / 原著硬约束 / 可提前铺垫 / 信息可见性` 里复原关键角色当前知道什么、不知道什么、误会什么、情感朝向是什么；后台推进只能顺着当前层级发酵，不把误会、自卫、亏欠、担心等中间态直接放大成血仇。',
    '- 再读 `【下一章节内容】`：确认下一章需要的世界起点、时间前瞻和禁止抢跑点，本回合最多只写预热、伏动、探查、调度或气压累积。',
    '- 当前章节未完成前，不把下一章节的事件结果、人物到位、地点变局或势力结论提前写成已发生事实。',
    ...(options?.fandom === true
        ? ['- 同人模式下，若下一章节涉及原著关键节点，本回合最多只写“逼近 / 发酵 / 预热 / 偏转准备”，不越级结算。',
            '- 同人模式下，若当前章只支持误会、自卫、防备、担忧、亏欠或未说开，就把后台人物动作收束在搜寻、观望、求证、躲避、迟疑、内疚或试探层级，不越级写成全面追杀、血仇或不死不休。']
        : []),
    '',
    'Step4: 活跃 NPC 推进与条数维护',
    '- 对仍需追踪的 NPC，确认其 `当前行动` 是否具体、`行动开始时间 / 行动结束时间` 是否闭合、`当前状态` 是否需要刷新。',
    '- 活跃 NPC 只记录已经开始执行的后台行动；若某条线索其实还没开始、仍在等条件或仍在预热，就不要 push 到这里，而应留在待执行事件或镜头规划中。',
    '- 若某条行动已经开始，就对照 `行动结束时间` 补推进结果；若时间已到仍未形成结果，就明确写成续段、受阻、改道或迁移，不让其长期空转。',
    '- 若某条 NPC 行动已经过期、失效或被台前打断，就判断是迁移、偏转、结算还是清理，并保留最小可追溯痕迹。',
    '- 若该 NPC 本回合已经在场、正在与主角直接互动，或后台动作被台前剧情截住，就把该后台项 `set` 成 `当前状态="[打断]当前在场"` 一类状态；`[打断]当前在场` 只属于状态字段，不写进 `当前行动`。`当前行动` 只保留被打断前正在执行的后台动作说明或当前的受阻描述，不再按正常后台推进。',
    '- 该 NPC 离场后若仍需继续跟踪，命令顺序固定为：先完成仍需保留的 `set/add`，再重新 `push 世界.活跃NPC列表 = {完整新对象}`，最后 `delete` 旧打断项；不要直接沿用旧打断项续写。',
    '- 审计普通远端 `世界.活跃NPC列表` 条数：常态建议维持 7 条、峰值不超过 9 条；条数不足时优先补已有旧线、势力余波、追缉链、探查线、护送线、封锁线与女主远端动向。',
    '- 只有当 NPC 具备“可验证来源 + 当前行动 + 行动开始时间 + 行动结束时间”四件套时，才值得占一个活跃槽位；缺任一项时先不新增。',
    '- `[女主]` 跟踪项单独做保留审计：女主不在场且远端线未断时优先保留；女主已回到台前时，改为打断、结算或移除。',
    '- 补位不是凑数：没有可信来源时，允许暂时低于常态，但要在说明层写清为什么不硬补。',
    ...(options?.fandom === true
        ? [
            '- 同人模式下，新增或刷新活跃 NPC 时，先判断其来源属于“原著已存在人物 / 原著势力链可合理外延人物 / 已被当前偏转事实明确生出的人物”哪一类，再决定推进尺度。',
            '- 同人模式下，原著角色的后台行动要过五项复核：原著时期、身份立场、情报能力、行动半径、当前章节窗口；不通过时回退为观察、预热、余波或不写。']
        : []),
    '',
    'Step5: 世界事件推进与并发维护',
    '- 把事件候选拆成“保持待执行 / 启动进行中 / 继续发酵 / 结算归档 / 错过偏转 / 失效清理”六类，逐项判断最合适的落点。',
    '- 只有时间门槛与条件门槛同时成立时，才允许把 `待执行事件` 推进到 `进行中事件`。',
    '- 只有结果已经落定时，才允许把 `进行中事件` 迁入 `已结算事件`；只有长期影响已经成立时，才允许再补入 `江湖史册`。',
    '- 审计普通 `世界.进行中事件` 条数：常态建议维持 5 条、峰值不超过 7 条；不足时优先从既有势力矛盾、剧情余波、传闻链、追缉链和预备动向中补位。',
    '- 对 `世界.进行中事件` 的补位先做来源排序：先看已满足条件的待执行事件，再看既有旧事件的发酵续段，再看当前章逼近出的世界前置状态，最后才看强因果的新动向。',
    '- 进入 `世界.进行中事件` 的对象必须至少同时具备“开始原因 / 当前进展 / 已产生影响”三项；不完整时先停留在待执行、观察或预热层，不拿空壳事件占位。',
    '- `世界.进行中事件[i].当前进展` 只允许保留一条当前摘要；若事件推进了，先吸收旧进展，再把该字段 `set` 成新的摘要，不追加旧日志。',
    '- 若普通 `世界.进行中事件` 超过 7 条，本 Step 必须把它压回 7 条以内：优先结算已落定项，再回退失效或空转项，最后清最旧、最弱、最远离当前章的项。',
    '- 审计普通 `世界.已结算事件` 条数：常态建议维持 4 条、峰值不超过 6 条；它只保留近期结算缓冲层，不承担长期仓储职责。',
    '- 若 `世界.已结算事件` 超额，就在本 Step 先做定量清理预案：先迁史册可归档项，再删结果已沉淀的普通项，若仍超额，再按“最旧优先、最弱优先、与当前章最远优先”继续清到阈值内。',
    '- 若当前没有足够可信的事件来源，就宁可低于常态，并在说明层写清“为什么不凑数”。',
    ...(options?.fandom === true
        ? ['- 同人模式下，事件补位优先承接原著事件链、原著势力冲突和原著余波；若只形成远端波动，就保持在逼近、筹备、发酵或旁支余波层。']
        : []),
    '',
    'Step6: 世界镜头与史册维护清理',
    '- 固定审计 `世界.世界镜头规划` 与 `世界.江湖史册`，把它们当维护池而不是仓库；凡是过期、失效、已被吸收或长期无后续价值的内容，都要在这里处理掉。',
    '- 对 `世界.世界镜头规划`，区分应继续保留的后台底噪、应转成已发生余波的镜头、以及触发窗口已过、已被正文/世界承接、已沉淀进其他结构、与当前章节长期脱节的失效镜头。',
    '- 镜头必须服务章节氛围、世界压力、女主远端动向和后续承接；只剩重复信息、空壳提醒或无引用价值时，直接改状态、迁移或删除，不为了凑数量而存在。',
    '- 对 `世界.江湖史册`，只保留真正会被后续长期引用的条目：长期格局变化、势力秩序改写、重要人物去向改写、关键传承易手、明确分歧线成立等。',
    '- 临时波动、一次性噪声、误入史册的短时事件、结果已被现行世界/剧情充分吸收且后续几乎不会再引用的弱条目，要从 `世界.江湖史册` 清出，不让史册堆成垃圾堆。',
    '- 若某结算事件虽已结束但长期影响尚未成立或信息仍不完整，就先留在 `世界.已结算事件` 观察，不急着塞进史册；已经完成观察、沉淀或归档职责的旧结算项则继续迁出或删除。',
    '',
    'Step7: 地图、建筑与地点联动',
    '- 若本回合形成新的可独立抵达地点，就优先考虑写入 `世界.地图`；若形成的是地点内部微观构造，再考虑写入 `世界.建筑`。',
    '- 坐标、归属、描述要与当前环境和世界状态一致，不制造空间冲突。',
    '- 若线索只是地点内部细节或临时站位变化，就保持在环境或前台事实层，不强行新增地图节点。',
    '- 若地点变化将影响后续导航、追缉、护送、封锁或势力活动，就优先写成可追踪的地点结构。',
    '',
    'Step8: 环境层联动审计',
    '- 复核天气、地点气氛、临时环境变量是否确有直接原因与持续区间。',
    '- 区分“世界层变化带来的环境承接”与“主链已落地命令已直接更新过的环境信息”，避免重复写入。',
    '- 对只属氛围推测、缺少证据的环境变化，一律保持不改。',
    '- 环境变量始终保持精简，只保留当前最影响行动的 1 到 2 条。',
    '',
    'Step9: 候选命令预演',
    '- 把拟执行变化整理成“路径 -> 操作 -> 前值 -> 后值 -> 依据”的最小命令候选。',
    '- 检查每条候选命令是否已精确到最小路径，尤其避免整对象覆盖、重复写入和跨池混写。',
    '- 对 `set / push / delete` 的选择做一次合法性预演，确认不会破坏现有结构或制造半残对象。',
    '- 命令顺序固定为“先 `set/add`，再 `push`，最后 `delete`”；若同一数组存在多个 `delete`，继续按索引从大到小预演。',
    '- 对条数维护类候选，单独说明补位来源、后续价值与为何值得占一个槽位。',
    '',
    'Step10: 迁移、清理与顺序复核',
    '- 涉及事件迁移时，顺序优先为“补结果 -> 推入新池 -> 删除旧池”，保证迁移链闭合。',
    '- 涉及 NPC、镜头、环境清理时，先确认其承接价值是否真的消失，再决定删除或改状态。',
    '- 若信息不足以完成整条迁移，就先做最小状态修订，不强行半结构迁移。',
    '- 若条数已超峰值，优先处理失效、空转、已无后续价值的旧项，再决定是否保留新项。',
    '- 对 `世界.已结算事件`，若超出峰值，本 Step 必须把删除/归档顺序排成可执行命令，并确保最终数量回到峰值以内；不能只给原则，不给清理结果。',
    '',
    'Step11: 证据强度与一致性复核',
    '- 逐项检查每个候选变化是否有足够前台事实证据、时间依据、结构依据、章节依据与承接价值。',
    '- 复核更新后是否仍保持：时序正确、章节防抢跑成立、无重复无冲突、同名对象单线跟踪、普通条数处于合理范围。',
    '- 若发现写入会制造新断档、新冲突或越权字段，就回退到更小改动、保持观察或取消更新。',
    ...(options?.fandom === true
        ? ['- 同人模式下，再复核原著角色是否 OOC、原著势力是否被无依据改性、原著事件顺位是否被无依据提前改写、原著角色是否被赋予超出当前时期的情报能力或行动半径；不通过就回退到更保守层级。']
        : []),
    '',
    'Step12: 输出说明成型',
    '- 把本轮更新理由整理成可复核的说明：哪些是到期推进，哪些是迁移归档，哪些是条数补位，哪些是环境承接，哪些明确保持观察。',
    '- 若无更新，说明必须写清“为什么不用改”，而不是只写一句空泛结论。',
    '- 若普通条数暂时低于常态，也要在说明里写清“当前为何宁可留缺也不硬补”。',
    '- 说明只写依据与处理方向，不写散文化解释。',
    '',
    'Step13: 输出结构复核',
    '- 检查最终是否仍只输出 `<thinking>`、`<说明>`、`<命令>` 三个标签块，且顺序固定。',
    '- 若无更新，确认 `<说明>` 写成可复核的无需更新理由，`<命令>` 输出 `无`。',
    '- 说明块与命令块彼此一致，让“说不更新却仍下命令”的冲突停留在输出之外。',
    '- 再次清点普通远端活跃 NPC 是否回到 7 条左右、普通进行中事件是否回到 5 条左右；若未达标，说明原因已经在说明层写明。',
    '- 若普通 `世界.进行中事件` 仍超过 7 条，当前输出直接视为不合格，必须回退重排直到数量回到阈值内。',
    '- 再次清点普通 `世界.已结算事件` 是否回到 4 条左右且不超过 6 条；若仍超额，说明与命令都视为不合格，需要回退重排。',
    '',
    'Step14: 命令落地预检',
    '- 再次检查每条命令是否落在最小路径，尤其确认镜头清理、史册清理、事件迁移与 NPC 刷新之间没有顺序冲突。',
    '- 若同一对象既要补字段又要迁移/删除，先保证结构完整与迁移闭环，再落最终清理，不留下半残中间态。',
    '- 对本回合新增的清理命令，复核它们删除的确实是过期、失效、已吸收或长期无引用价值的项，而不是仍应保留的后续锚点。',
    '- 若某条清理命令会打断仍在运作的后续链，就回退到改状态、迁移或继续观察层，而不是直接硬删。',
    '',
    'Step15: 命令落地',
    '- 命令格式固定：`add|set|push|delete 路径 = 值`。',
    '- 最终输出顺序固定为“先 `set/add`，再 `push`，最后 `delete`”；若同一数组存在多个 `delete`，按索引从大到小输出。',
    '- 命令必须使用最小路径，例如 `世界.待执行事件[0].当前状态`、`世界.活跃NPC列表[1].当前行动`、`环境.天气`。',
    '- 输出时直接写 `世界.*` 或 `环境.*` 路径，让结果可直接进入下一轮世界维护。',
    '- 若存在 `[女主]` 跟踪项、条数补位或史册归档，命令必须明确落到对应对象和字段，不把其停留在说明层。',
    '',
    '</世界演变思考协议>'
].join('\n');

export const 世界演变COT提示词 = 构建世界演变COT提示词();

export const 世界演变COT伪装历史消息提示词 = `<think>
思考已结束
</think>
好的，将先以<thinking></thinking>完成世界维护思考，再以<说明></说明>输出后台世界维护摘要，最后以<命令></命令>输出世界与环境命令：`;
